# 19单调队列

## 题单

洛谷：中等 * 1;  

力扣：中等 * 1;	困难 * 4;  

### 1）经典模板

1. [239. 滑动窗口最大值](https://leetcode.cn/problems/sliding-window-maximum/) (hard)
2. [1438. 绝对差不超过限制的最长连续子数组](https://leetcode.cn/problems/longest-continuous-subarray-with-absolute-diff-less-than-or-equal-to-limit/) (medium)
3. [USACO12MAR\] Flowerpot S   ](https://www.luogu.com.cn/problem/P2698) (普及+/提高-)

### 2）扩展用法

1. [862. 和至少为 K 的最短子数组](https://leetcode.cn/problems/shortest-subarray-with-sum-at-least-k/) (hard)
2. [1499. 满足不等式的最大值](https://leetcode.cn/problems/max-value-of-equation/) (hard)
3. [2071. 你可以安排的最多任务数目](https://leetcode.cn/problems/maximum-number-of-tasks-you-can-assign/) (hard)

## 笔记

1. 单调队列最经典的用法是解决如下问题：

   > * 滑动窗口在滑动时，r++代表右侧数字进窗口，l++代表左侧数字出窗口
   > * 这个过程中，想随时得到当前滑动窗口的 最大值 或者 最小值
   > * 窗口滑动的过程中，单调队列所有调整的总代价为O(n)，单次操作的均摊代价为O(1)

2. 除了单调队列最经典的用法之外，在很多问题里单调队列还可以 维持求解答案的可能性

   > * 单调队列里的所有对象按照 规定好的单调性来组织
   > * 当某个对象从队尾进入单调队列时，
   >      会从 队头 或者 队尾 依次淘汰单调队列里，对后续求解答案没有帮助 的对象
   > * 每个对象一旦从单调队列弹出，可以结算此时这个对象参与的答案，
   >      随后这个对象 不再参与后续求解答案的过程
   > * 其实是 先有对题目的分析！进而发现单调性，然后利用 单调队列的特征 去实现

## code

### 1）经典模板

1. [239. 滑动窗口最大值](https://leetcode.cn/problems/sliding-window-maximum/) (hard)

   > 给你一个整数数组 `nums`，有一个大小为 `k` 的滑动窗口从数组的最左侧移动到数组的最右侧。你只可以看到在滑动窗口内的 `k` 个数字。滑动窗口每次只向右移动一位。
   >
   > 返回 *滑动窗口中的最大值* 。

   ```c++
   class Solution {
       static const int MAXN = 100001;
       inline static array<int, MAXN> deque = {};
   public:
       vector<int> maxSlidingWindow(vector<int>& nums, int k) {
           // 形成大小为k的窗口
           int h = 0, t = 0;
           for (int i = 0; i < k; i++) {
               // 维持队列单调减
               while (h < t && nums[deque[t - 1]] <= nums[i]) {
                   t--;
               }
               deque[t++] = i; // 索引进队列
           }
           // 收集答案数
           int n = nums.size();
           int m = n - k + 1;
           vector<int> ans(m);
           for (int i = 0, r = k; i < m; i++, r++) { // r:当前窗口已有k大小
               ans[i] = nums[deque[h]]; // 收集答案, 队列头对应元素
               // 右滑窗口
               while (h < t && r < n && nums[deque[t - 1]] <= nums[r]) {
                   t--;
               }
               deque[t++] = r;
               // 检查队列头是否过期
               if (deque[h] < i + 1) {
                   h++;
               }
           }
           return ans;
       }
   };
   ```

2. [1438. 绝对差不超过限制的最长连续子数组](https://leetcode.cn/problems/longest-continuous-subarray-with-absolute-diff-less-than-or-equal-to-limit/) (medium)

   > **思路：** 
   >
   > 1. 滑动窗口，过程中维护2个单调队列，一个用于窗口最大值，一个用于最小值。
   > 2. 检查最大最小值的差是否超过 `limit` ，若超过了就移动左指针缩小窗口。
   > 3. 缩小窗口后，检查2个单调队列头是否过期，过期则出队列；

   ```c++
   class Solution {
       static const int MAXN = 100001;
       inline static array<int, MAXN> maxdq = {};
       inline static array<int, MAXN> mindq = {};
       int maxh, maxt, minh, mint;
   public:
       int longestSubarray(vector<int>& nums, int limit) {
           int n = nums.size();
           int ans = 0;
           maxh = maxt = minh = mint = 0;
           for (int l = 0, r = 0; r < n; r++) {
               // 新元素进队列, 更新答案
               inq(nums, r);
               while (nums[maxdq[maxh]] - nums[mindq[minh]] > limit) {
                   l++; // 移动左指针
                   outq(nums, l);
               }
               ans = max(ans, r - l + 1);
           }
           return ans;
       }
   
       void inq(vector<int>& nums, int i) {
           // 维护2个单调队列, 索引进队列
           while (maxh < maxt && nums[maxdq[maxt - 1]] <= nums[i]) {
               maxt--;
           }
           maxdq[maxt++] = i;
           while (minh < mint && nums[mindq[mint - 1]] >= nums[i]) {
               mint--;
           }
           mindq[mint++] = i;
       }
   
       // i位置过期，检查是否过期
       void outq(vector<int>& nums, int i) {
           if (maxdq[maxh] < i) {
               maxh++;
           }
           if (mindq[minh] < i) {
               minh++;
           }
       }
   };
   ```

3. [USACO12MAR\] Flowerpot S   ](https://www.luogu.com.cn/problem/P2698) (普及+/提高-)

   > ![](img/9174.png) 
   >
   > 老板需要你帮忙浇花。给出 $N$ 滴水的坐标，$(x,y)$ 表示水滴最初的坐标。
   >
   > 每滴水均以每秒 $1$ 个单位长度的速度下落。你需要把花盆放在 $x$ 轴上的某个位置，使得花盆接到第 $1$ 滴水与最后 $1$ 滴水之间的时间差至少为 $D$。
   >
   > 如果水滴落在 $x$ 轴上的位置与花盆的边沿对齐，也认为被接住。
   >
   > 给出 $N$ 滴水的坐标和时间差 $D$ ，请算出最小的花盆宽度 $W$。
   >
   > **输入格式** 
   >
   > 第一行 $2$ 个整数 $N$ 和 $D$。
   >
   > 接下来 $N$ 行，每行 $2$ 个整数，表示水滴的坐标 $(x,y)$。
   >
   > **输出格式** 
   >
   > 一行 $1$ 个整数，表示最小的花盆宽度。如果无法构造出满足题意的花盆，则输出 $-1$。
   >
   > **说明/提示** 
   >
   > **【样例解释】** 
   >
   > 有 $4$ 滴水，初始位置分别在 $(6,3)$，$(2,4)$，$(4,10)$，$(12,15)$。水滴至少用 $5$ 秒时间先后落入花盆。花盆的宽度为 $2$ 是必须且足够的，此时把花盆放在 $x=4\dots6$ 的位置，它可以接到水滴 $1$ 和 $3$ ，之间的时间差为 $10-3=7$，满足条件。
   >
   > **【数据范围】** 
   >
   > $40\%$ 的数据：$1 \le N \le 1000$ ，$1 \le D \le 2000$。
   >
   > $100\%$ 的数据：$1 \le N \le 10 ^ 5$，$1 \le D \le 10 ^ 6$，$0\le x,y\le10^6$。

   ```c++
   #include <iostream>
   #include <algorithm>
   #include <array>
   #include <climits>
   
   using namespace std;
   
   enum {
       MAXN = 100001
   };
   static array<array<int, 2>, MAXN> drops = {};
   static array<int, MAXN> maxdq = {};
   static array<int, MAXN> mindq = {};
   int maxh, maxt, minh, mint;
   void inq(int i);
   int dif();
   void outq(int i);
   int lstlen(int n, int d);
   
   int main() {
       ios::sync_with_stdio(false);
       cin.tie(nullptr);
       int n, d; // n滴水, 限制d
       while (cin >> n >> d) {
           for (int i = 0; i < n; i++) {
               cin >> drops[i][0] >> drops[i][1];
           }
           int ans = lstlen(n, d);
           ans = ans != INT_MAX ? ans : -1;
           cout << ans << '\n';
       }
   }
   
   int lstlen(int n, int d) {
       // 根据x大小升序排序, 只排序有效坐标
       // 对雨滴离散化处理, 用索引表示雨滴
       sort(drops.begin(), drops.begin() + n, 
            [](const array<int, 2>& a, const array<int, 2>& b) {
           return a[0] < b[0];
       });
       maxh = maxt = minh = mint = 0;
       int ans = INT_MAX;
       for (int l = 0, r = 0; l < n; l++) { // 窗口左侧从l开始
           while (dif() < d && r < n) { // 什么时候满足条件
               inq(r++);
           }
           if (dif() >= d) { // 满足才更新答案
               ans = min(ans, drops[r - 1][0] - drops[l][0]); // 水滴x坐标差
           }
           outq(l);
       }
       return ans;
   }
   
   // 进单调队列
   void inq(int i) {
       while (maxh < maxt && drops[maxdq[maxt - 1]][1] <= drops[i][1]) {
           maxt--;
       }
       maxdq[maxt++] = i;
       while (minh < mint && drops[mindq[mint - 1]][1] >= drops[i][1]) {
           mint--;
       }
       mindq[mint++] = i;
   }
   
   int dif() {
       // 队列若为空，则差值视为0
       int max = maxh < maxt ? drops[maxdq[maxh]][1] : 0;
       int min = minh < mint ? drops[mindq[minh]][1] : 0;
       return max - min;
   }
   
   void outq(int i) {
       // 淘汰过期坐标i
       if (maxh < maxt && maxdq[maxh] <= i) {
           maxh++;
       }
       if (minh < mint && mindq[minh] <= i) {
           minh++;
       }
   }
   ```

   

### 2）扩展用法

1. [862. 和至少为 K 的最短子数组](https://leetcode.cn/problems/shortest-subarray-with-sum-at-least-k/) (hard)

   > 给你一个整数数组 `nums` 和一个整数 `k` ，找出 `nums` 中和至少为 `k` 的 **最短非空子数组** ，并返回该子数组的长度。如果不存在这样的 **子数组** ，返回 `-1` 。
   >
   > **子数组** 是数组中 **连续** 的一部分。
   >
   > **提示：**
   >
   > - `1 <= nums.length <= 10^5` 
   > - `-105 <= nums[i] <= 10^5` 
   > - `1 <= k <= 10^9` 

   > **思路：** 
   >
   > 1. 因为数组有负数，所以用滑动窗口不满足单调性，选用单调队列维护前缀和数组，单调队列内元素严格单调递增，维持求解答案的可能性。
   > 2. 当前数组结尾位置 `i`，前缀和为 `prefix[i]` ，要使和大于等于 `k`，则找最靠右的前缀和小于等于 `prefix[i] - k` 的索引，找到的用于更新答案，且出队列，因为再以这个索引开始找到的答案长度会更长，不会比当前更优。
   > 3. 当新的前缀和数组索引 `i` 进入单调队列，若比队尾的前缀和小，则淘汰队尾，因为这个索引开始找的数组和更大且长度更短，对于找答案更优。

   ```c++
   class Solution {
       static const int MAXN = 100001;
       inline static array<long long, MAXN> prefix = {};
       inline static array<int, MAXN> dq = {};
       int h, t;
   public:
       int shortestSubarray(vector<int>& nums, int k) {
           int n = nums.size();
           prefix[0] = 0;
           for (int i = 0; i < n; i++) {
               prefix[i + 1] = prefix[i] + nums[i]; // 前i个数前缀和
           }
           int ans = INT_MAX;
           for (int i = 0; i <= n; i++) {
               // 前i个数和 减 前dq[h]个数前缀和达标
               // 记录答案, 并且队列头出
               while (h < t && prefix[i] - prefix[dq[h]] >= k) {
                   ans = min(ans, i - dq[h++]);
               }
               // i前缀索引进队列, 保持严格单调增
               while (h < t && prefix[dq[t - 1]] >= prefix[i]) {
                   t--;
               }
               dq[t++] = i;
           }
           return ans != INT_MAX ? ans : -1;
       }
   };
   ```

2. [1499. 满足不等式的最大值](https://leetcode.cn/problems/max-value-of-equation/) (hard)

   > 给你一个数组 `points` 和一个整数 `k` 。数组中每个元素都表示二维平面上的点的坐标，并按照横坐标 x 的值从小到大排序。也就是说 `points[i] = [xi, yi]` ，并且在 `1 <= i < j <= points.length` 的前提下， `xi < xj` 总成立。
   >
   > 请你找出 `yi + yj + |xi - xj|` 的 **最大值**，其中 `|xi - xj| <= k` 且 `1 <= i < j <= points.length`。
   >
   > 题目测试数据保证至少存在一对能够满足 `|xi - xj| <= k` 的点。
   >
   > **提示：**
   >
   > - `2 <= points.length <= 105`
   > - `points[i].length == 2`
   > - `-108 <= xi, yi <= 108`
   > - `0 <= k <= 2 * 108`
   > - 对于所有的 `1 <= i < j <= points.length`，均有 `xi < xj`。
   > - `xi` 构成一个严格递增序列。

   > **思路：** 
   >
   > 1. `xi`严格递增.所以变成`xj - xi`.去掉绝对值,
   >
   >    维护 `max(yi - xi)`. 注意到 `k` 是定值.所以这样的维护只需要单调队列即可.
   >
   > 2. 单调队列存 `points` 下标，维护 `max(yi - xi)` 的可能性

   ```c++
   class Solution {
       static const int MAXN = 100001;
       inline static array<int, MAXN> deque = {};
       int head, tail;
   public:
       int findMaxValueOfEquation(vector<vector<int>>& points, int k) {
           //xi严格递增.所以变成xj - xi.去掉绝对值
           //维护max(yi - xi).注意到k是定值.所以这样的维护只需要单调队列即可.
           head = tail = 0;
           int ans = INT_MIN;
           for (int i = 0; i < points.size(); i++) {
               int xj = points[i][0];
               int yj = points[i][1];
               // 使xj - xi <= k
               while (head < tail && xj - points[deque[head]][0] > k) {
                   head++;
               }
               if (head < tail) {
                   ans = max(ans, 
                   points[deque[head]][1] + yj + xj - points[deque[head]][0]);
               }
               // 当前point索引进队列, 维护max(yi - xi)
               while (head < tail && 
               points[deque[tail - 1]][1] - points[deque[tail - 1]][0] 
               <= yj - xj) {
                   tail--;
               }
               deque[tail++] = i;
           }
           return ans;
       }
   };
   ```

3. [2071. 你可以安排的最多任务数目](https://leetcode.cn/problems/maximum-number-of-tasks-you-can-assign/) (hard)

   > 给你 `n` 个任务和 `m` 个工人。每个任务需要一定的力量值才能完成，需要的力量值保存在下标从 **0** 开始的整数数组 `tasks` 中，第 `i` 个任务需要 `tasks[i]` 的力量才能完成。每个工人的力量值保存在下标从 **0** 开始的整数数组 `workers` 中，第 `j` 个工人的力量值为 `workers[j]` 。每个工人只能完成 **一个** 任务，且力量值需要 **大于等于** 该任务的力量要求值（即 `workers[j] >= tasks[i]` ）。
   >
   > 除此以外，你还有 `pills` 个神奇药丸，可以给 **一个工人的力量值** 增加 `strength` 。你可以决定给哪些工人使用药丸，但每个工人 **最多** 只能使用 **一片** 药丸。
   >
   > 给你下标从 **0** 开始的整数数组`tasks` 和 `workers` 以及两个整数 `pills` 和 `strength` ，请你返回 **最多** 有多少个任务可以被完成。
   >
   > **提示：**
   >
   > - `n == tasks.length`
   > - `m == workers.length`
   > - `1 <= n, m <= 5 * 104`
   > - `0 <= pills <= m`
   > - `0 <= tasks[i], workers[j], strength <= 109` 

   > **思路：** 
   >
   > 1. 二分下界为0个任务，上界为 `min(tn, wn)` 。
   > 2. 想完成 `mid` 个任务，找最小的 `mid` 个任务和最大的 `mid` 个工人：
   >    * 当前工人能完成的任务加入双端队列；
   >    * 条件允许，完成双端队列中最小的任务就行；
   >    * 一个能完成的任务都没有，就吃药，再把能完成的任务加入双端队列；
   >    * 为了不浪费药，完成能完成的最大的一个任务；
   > 3. 这是最省药的方式，若中途仍然不能完成任务，或最后用药超了，就说明无法完成 `mid` 个任务

   ```c++
   cclass Solution {
       static const int MAXN = 50001;
       inline static array<int, MAXN> deque = {};
       int h, t;
   public:
       int maxTaskAssign(vector<int>& tasks, vector<int>& workers, int pills, int strength) {
           // 排序，便于检查是否能完成mid个任务
           sort(tasks.begin(), tasks.end());
           sort(workers.begin(), workers.end());
           int tn = tasks.size();
           int wn = workers.size();
           // 二分下界为0个任务，上界为 min(tn, wn)
           int flr = 0, ceil = min(tn, wn), mid;
           int ans;
           while (flr <= ceil) {
               mid = flr + ((ceil - flr) >> 1);
               // 能完成mid个任务，记录答案，往右侧二分
               // 找最小mid个任务和最大mid个工人
               if (cando(tasks, workers, pills, strength, mid, 0, mid - 1, wn - mid, wn - 1)) {
                   ans = mid;
                   flr = mid + 1;
               } else {
                   ceil = mid - 1;
               }
           }
           return ans;
       }
   
       bool cando(vector<int>& tasks, vector<int>& workers, int pills, int strength, int num, int tl, int tr, int wl, int wr) {
           int cnts = 0;
           h = t = 0;
           for (int i = wl, j = tl; i <= wr; i++) { // 工人编号
               // 工人能解锁的任务
               while (j <= tr && tasks[j] <= workers[i]) {
                   deque[t++] = j++;
               }
               // 队列中有能做的，把最小的任务完成
               if (h < t && tasks[deque[h]] <= workers[i]) {
                   h++;
               } else {
                   // 不能做任务，就吃药
                   while (j <= tr && tasks[j] <= workers[i] + strength) {
                       deque[t++] = j++;
                   }
                   // 为了不浪费药，做能做的最大任务
                   // 吃药的工人一定能完成队列中已经解锁的最大任务
                   if (h < t) {
                       cnts++;
                       t--;
                   } else {
                       return false;
                   }
               }
           }
           // 按最省药片数量超了，就说明无法完成
           return cnts <= pills;
       }
   };
   ```

   