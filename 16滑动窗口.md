# 16滑动窗口

## 题单

力扣: 中等 * 5;	困难 * 2;

1. [209. 长度最小的子数组](https://leetcode.cn/problems/minimum-size-subarray-sum/)（medium）
2. [3. 无重复字符的最长子串](https://leetcode.cn/problems/longest-substring-without-repeating-characters/) (medium)
3. [76. 最小覆盖子串](https://leetcode.cn/problems/minimum-window-substring/) (hard)
4. [134. 加油站](https://leetcode.cn/problems/gas-station/) (medium)
5. [1234. 替换子串得到平衡字符串](https://leetcode.cn/problems/replace-the-substring-for-balanced-string/) (medium)
6. [992. K 个不同整数的子数组](https://leetcode.cn/problems/subarrays-with-k-different-integers/) (hard)
7. [395. 至少有 K 个重复字符的最长子串](https://leetcode.cn/problems/longest-substring-with-at-least-k-repeating-characters/) (medium)

## 笔记

### 1）必须具有“决策单调性”（核心数学条件）

这是决定能否使用滑动窗口的**灵魂**。它指的是：**窗口的可行性（或最优性）随着左指针（L）或右指针（R）的移动具有单调的变化趋势。**

- **左端点移动的单调性**：当右指针 `R` 固定时，左指针 `L` 向右移动，窗口**变小**，其具备的某种性质（如元素和、种类数）一定**不增**（变小或不变）。
- **右端点移动的单调性**：`R` 向右移动，窗口**变大**，其性质一定**不减**（变大或不变）。

> **举个例子**：求“和 ≥ target 的最短子数组”。和具有单调性——加数变多，和一定变大；删掉左边，和一定变小。这种单调性保证了当 `R` 右移导致窗口“太长了”时，我们只需不断右移 `L` 来找“刚好”的位置，而无需让 `L` 回退。

### 2）窗口状态必须具有“可撤销性”（即“可加/可减性”）

当左指针 `L` 向右移动（收缩窗口）时，你**必须能够通过 O(1) 或 O(log n) 的代价**，准确地将该元素从窗口状态中移除。

- **如果状态是“和”或“频次”**：直接 `sum -= nums[L]` 或 `cnt[char]--`，极易撤销 ✅。
- **如果状态是“最大值”或“最小值”**：左指针移除元素后，如果你无法确定当前窗口的新最大值是谁（除非重新遍历窗口），那么**标准的左右双指针滑动窗口就不适用**。这种情况需要改用**单调队列**（如 `deque`）来辅助，这属于滑动窗口的进阶变种。

## code

1. [209. 长度最小的子数组](https://leetcode.cn/problems/minimum-size-subarray-sum/)（medium）

   > 找出该数组中满足其总和大于等于 `target` 的长度最小的 **子数组** `[numsl, numsl+1, ..., numsr-1, numsr]` ，并返回其长度**。**如果不存在符合条件的子数组，返回 `0` 。

   ```c++
   class Solution {
   public:
       int minSubArrayLen(int target, vector<int>& nums) {
           int ans = INT_MAX;
           for (int l = 0, r = 0, sum = 0; r < nums.size(); r++) {
               sum += nums[r]; // 右指针元素进窗口
               // 滑窗
               while (sum - nums[l] >= target) { // 左指针出去后仍然满足
                   sum -= nums[l++]; // 左指针元素出窗
               }
               if (sum >= target) {
                   ans = min(ans, r - l + 1); // 更新答案
               } 
           }
           return ans != INT_MAX ? ans : 0;
       }
   };
   ```

2. [3. 无重复字符的最长子串](https://leetcode.cn/problems/longest-substring-without-repeating-characters/) (medium)

   ```c++
   class Solution {
   public:
       int lengthOfLongestSubstring(string s) {
           vector<int> cidx(256, -1); // 记录字符上次出现下标，初始为-1
           int ans = 0;
           for (int l = 0, r = 0; r < s.size(); r++) {
               l = max(l, cidx[s[r]] + 1); // 滑动窗口
               ans = max(ans, r - l + 1);
               cidx[s[r]] = r; // 更新当前右指针字符的索引
           }
           return ans;
       }
   };
   ```

3. [76. 最小覆盖子串](https://leetcode.cn/problems/minimum-window-substring/) (hard)

   > 给定两个字符串 `s` 和 `t`，长度分别是 `m` 和 `n`，返回 s 中的 **最短窗口 子串**，使得该子串包含 `t` 中的每一个字符（**包括重复字符**）。如果没有这样的子串，返回空字符串 `""`。
   >
   > 测试用例保证答案唯一。

   > **思路：** 
   >
   > 1. 负债模型：子串中需要包含的字符及个数加入表中，字串的长度及总负债
   > 2. 窗口右边新加入的字符若是负债的字符，则可以还债
   > 3. 当总债务清0时，可以尝试使左指针右移，减小窗口

   ```c++
   class Solution {
   public:
       string minWindow(string s, string t) {
           int n = t.size();
           int cnts[256] = {}; // 记录字符负债
           for (int i = 0; i < n; i++) {
               cnts[t[i]]--;
           }
           int debt = n; // t的长度，总负债
           int start = -1, len = INT_MAX;
           for (int l = 0, r = 0; r < s.size(); r++) {
               // 新进来的是欠债，还债
               if (cnts[s[r]] < 0) {
                   debt--;
               }
               cnts[s[r]]++;
               // 当前窗口包含t的所有字符
               if (debt == 0) {
                   // 左指针有盈余，可以右移
                   while (cnts[s[l]] > 0) {
                       cnts[s[l++]]--;
                   }
                   // 更新答案
                   if (r - l + 1 < len) {
                       len = r - l + 1;
                       start = l;
                   }
               }
           }
           return len != INT_MAX ? s.substr(start, len) : "";
       }
   };
   ```

4. [134. 加油站](https://leetcode.cn/problems/gas-station/) (medium)

   > 在一条环路上有 `n` 个加油站，其中第 `i` 个加油站有汽油 `gas[i]` 升。
   >
   > 你有一辆油箱容量无限的的汽车，从第 `i` 个加油站开往第 `i+1` 个加油站需要消耗汽油 `cost[i]` 升。你从其中的一个加油站出发，开始时油箱为空。
   >
   > 给定两个整数数组 `gas` 和 `cost` ，如果你可以按顺序绕环路行驶一周，则返回出发时加油站的编号，否则返回 `-1` 。如果存在解，则 **保证** 它是 **唯一** 的。

   ```c++
   class Solution {
   public:
       int canCompleteCircuit(vector<int>& gas, vector<int>& cost) {
           int n = gas.size();
           for (int l = 0, r = 0, sum; l < n; l = r + 1, r = l) {
               sum = 0;
               // 当前净油量大于等于0，可以行驶到r+1个站
               while (sum + gas[r % n] - cost[r % n] >= 0) {
                   sum += gas[r % n] - cost[r % n];
                   r++;
                   // 从l开始完成了1轮循环
                   if (r % n == l) {
                       return l;
                   }
               }
           }
           return -1;
       }
   };
   ```

5. [1234. 替换子串得到平衡字符串](https://leetcode.cn/problems/replace-the-substring-for-balanced-string/) (medium)

   > 有一个只含有 `'Q', 'W', 'E', 'R'` 四种字符，且长度为 `n` 的字符串。
   >
   > 假如在该字符串中，这四个字符都恰好出现 `n/4` 次，那么它就是一个「平衡字符串」。
   >
   > 给你一个这样的字符串 `s`，请通过「替换一个子串」的方式，使原字符串 `s` 变成一个「平衡字符串」。
   >
   > 你可以用和「待替换子串」长度相同的 **任何** 其他字符串来完成替换。
   >
   > 请返回待替换子串的最小可能长度。
   >
   > 如果原字符串自身就是一个平衡字符串，则返回 `0`。

   ```c++
   class Solution {
   public:
       int balancedString(string s) {
           int n = s.size();
           int cnts[4] = {};
           for (int i = 0; i < n; i++) {
               cnts[tidx(s[i])]++;
           }
           // 负债模型，比 n / 4 多的就是负债 
           int debt = 0; // 比 n / 4 多的个数
           for (int& cn: cnts) {
               if (cn <= n / 4) {
                   cn = 0;
               } else {
                   cn = n / 4 - cn;
                   debt -= cn;
               }
           }
           int ans = INT_MAX;
           for (int l = 0, r = 0; r < n; r++) {
               if (cnts[tidx(s[r])]++ < 0) { // 加入的新元素有负债
                   debt--;
               }
               if (debt == 0) { // 负债清空
                   while (cnts[tidx(s[l])] > 0) { // 有盈余
                       cnts[tidx(s[l++])]--; // 缩小窗口
                   }
                   ans = min(ans, r - l + 1);
               }
           }
           return ans != INT_MAX ? ans : 0;
       }
   
       int tidx(char c) {
           switch (c) {
               case 'Q': return 0;
               case 'W': return 1;
               case 'E': return 2;
               case 'R': return 3;
               default: return 0;
           }
       }
   };
   ```

6. [992. K 个不同整数的子数组](https://leetcode.cn/problems/subarrays-with-k-different-integers/) (hard)

   > 给定一个正整数数组 `nums`和一个整数 `k`，返回 `nums` 中 「**好子数组」** 的数目。
   >
   > 如果 `nums` 的某个子数组中不同整数的个数恰好为 `k`，则称 `nums` 的这个连续、不一定不同的子数组为 **「好子数组 」**。
   >
   > - 例如，`[1,2,3,1,2]` 中有 `3` 个不同的整数：`1`，`2`，以及 `3`。
   >
   > **子数组** 是数组的 **连续** 部分。

   > **思路：** 
   >
   > 1. 滑动窗口维护的是“**最多 K 个不同**”的性质，而不是“恰好 K 个”。固定右端点 `r` 时，满足“恰好 K 个不同”的左端点通常是一个连续区间，可能有多个值，而当前窗口的左端点 `l` 只是这个区间的最左边界，即满足“最多 K 个”的最左端。直接检查 `st == k` 只会统计当前时刻窗口的左端点，忽略了其他同样满足条件的左端点，导致少计。
   > 2. 所以统计左端点 `l` 到右端点 `r` 间种数小于 `K` 的子数组个数，然后用容斥原理计算出恰好 `k` 个不同整数的子数组

   ```c++
   class Solution {
       enum {
           MAXN = 20001
       };
       int cnts[MAXN] = {};
   
       // 种树小于等于k的子数组个数
       int lksubarr(vector<int>& nums, int k) {
           fill(begin(cnts), end(cnts), 0);
           int st = 0, ans = 0;
           for (int l = 0, r = 0; r < nums.size(); r++) {
               // 原来这种数为0
               if (cnts[nums[r]]++ == 0) {
                   st++;
               }
               while (st > k) {
                   // 窗口缩小后有一种数个数变为0，窗口种类减少
                   if (--cnts[nums[l++]] == 0) {
                       st--;
                   }
               }
               ans += r - l + 1; // 以r结尾的子数组个数
           }
           return ans;
       }
   public:
       int subarraysWithKDistinct(vector<int>& nums, int k) {
           return lksubarr(nums, k) - lksubarr(nums, k - 1);
       }
   };
   ```

7. [395. 至少有 K 个重复字符的最长子串](https://leetcode.cn/problems/longest-substring-with-at-least-k-repeating-characters/) (medium)

   > **思路：** 
   >
   > 1. 新加入的字符可能使单调性被破坏：题目要求每种字符都要大于 `k` 个，加入新字符后，种数增加，但新字符数量为1，可能小于 `k`，无法满足要求。
   > 2. 所以每次滑窗限制当前子串只有 `rq` 种字符，满足单调性，若加入新字符使种类增加，则缩小窗口.

   ```c++
   class Solution {
       inline static array<int, 26> cnts = {};
   public:
       int longestSubstring(string s, int k) {
           int ans = 0;
           for (int rq = 1; rq <= 26; rq++) { // 循环判断包含字符的种类
               cnts.fill(0);
               for (int l = 0, r = 0, type = 0, satisfy = 0, id; r < s.size(); r++) {
                   // 判断新元素加入的影响
                   id = s[r] - 'a';
                   cnts[id]++;
                   if (cnts[id] == 1) {
                       type++;
                   }
                   if (cnts[id] == k) {
                       satisfy++;
                   }
                   // 缩窗
                   while (type > rq) { // 包含字符比当前应该包含字符多
                       id = s[l++] - 'a';
                       if (cnts[id] == 1) {
                           type--;
                       }
                       if (cnts[id] == k) {
                           satisfy--;
                       }
                       cnts[id]--;
                   }
                   if (satisfy == rq) {
                       ans = max(ans, r - l + 1);
                   }
               }
           }
           return ans;
       }
   };
   ```

   