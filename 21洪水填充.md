# 21洪水填充

## 题单

> 力扣：中等 * 2;	困难 * 2;  

1. [200. 岛屿数量](https://leetcode.cn/problems/number-of-islands/) (medium)
2. [130. 被围绕的区域](https://leetcode.cn/problems/surrounded-regions/) (medium)
3. [827. 最大人工岛](https://leetcode.cn/problems/making-a-large-island/) (hard)
4. [803. 打砖块](https://leetcode.cn/problems/bricks-falling-when-hit/) (hard)

## 笔记

> 洪水填充

1. 设置路径信息进行统计，
2. 路径信息不撤销，进行剪枝；
3. 实际遍历次数和样本规模一致。

## code

1. [200. 岛屿数量](https://leetcode.cn/problems/number-of-islands/) (medium)

   > 给你一个由 `'1'`（陆地）和 `'0'`（水）组成的的二维网格，请你计算网格中岛屿的数量。
   >
   > 岛屿总是被水包围，并且每座岛屿只能由水平方向和/或竖直方向上相邻的陆地连接形成。
   >
   > 此外，你可以假设该网格的四条边均被水包围。
   >
   > **提示：**
   >
   > - `m == grid.length`
   > - `n == grid[i].length`
   > - `1 <= m, n <= 300`
   > - `grid[i][j]` 的值为 `'0'` 或 `'1'`

   > **思路：** 
   >
   > 时间复杂度：$O(n * m)$ 
   >
   > 1. 设置路径信息进行统计，
   > 2. 路径信息不撤销，进行剪枝；
   > 3. 实际遍历次数和样本规模一致。

   ```c++
   class Solution {
   public:
       int numIslands(vector<vector<char>>& grid) {
           int ans = 0;
           for (int i = 0; i < grid.size(); i++) {
               for (int j = 0; j < grid[0].size(); j++) {
                   if (grid[i][j] == '1') {
                       ans++;
                       // dfs感染为'1'的相连岛屿
                       dfs(grid, i, j);
                   }
               }
           }
           return ans;
       }
   
       void dfs(vector<vector<char>>& grid, int i, int j) {
           if (i < 0 || i == grid.size() || j < 0 || j == grid[0].size() || grid[i][j] != '1') {
               return; // 越界或不为'1'，返回
           }
           grid[i][j] = '2'; // 遍历过的为'1'岛屿修改
           dfs(grid, i - 1, j);
           dfs(grid, i + 1, j);
           dfs(grid, i, j - 1);
           dfs(grid, i, j + 1);
       }
   };
   ```

2. [130. 被围绕的区域](https://leetcode.cn/problems/surrounded-regions/) (medium)

   > 给你一个 `m x n` 的矩阵 `board` ，由若干字符 `'X'` 和 `'O'` 组成，**捕获** 所有 **被围绕的区域**：
   >
   > - **连接：**一个单元格与水平或垂直方向上相邻的单元格连接。
   > - **区域：连接所有** `'O'` 的单元格来形成一个区域。
   > - **围绕：**如果一个区域中的所有 `'O'` 单元格都不在棋盘的边缘，则该区域被包围。这样的区域 **完全** 被 `'X'` 单元格包围。
   >
   > 通过 **原地** 将输入矩阵中的所有 `'O'` 替换为 `'X'` 来 **捕获被围绕的区域**。你不需要返回任何值。
   >
   > **提示：**
   >
   > - `m == board.length`
   > - `n == board[i].length`
   > - `1 <= m, n <= 200`
   > - `board[i][j]` 为 `'X'` 或 `'O'`

   > **思路：** 
   >
   > 1. 用洪水填充感染边缘区域的 `O`，标记为 `E`；
   > 2.  遍历矩阵board, 把非边缘区域的 `O` 修改为 `X`，边缘区域'O'被改为 `E`，还原回 `O`。

   ```c++
   class Solution {
   public:
       void solve(vector<vector<char>>& board) {
           // 边缘'O'区域dfs标记为'E'
           int m = board.size(); // 行
           int n = board[0].size(); // 列
           for (int j = 0; j < n; j++) {
               if (board[0][j] == 'O') {
                   dfs(board, 0, j);
               }
               if (board[m - 1][j] == 'O') {
                   dfs(board, m - 1, j);
               }
           }
           for (int i = 1; i < m - 1; i++) {
               if (board[i][0] == 'O') {
                   dfs(board, i, 0);
               }
               if (board[i][n - 1] == 'O') {
                   dfs(board, i, n - 1);
               }
           }
           // 遍历矩阵board, 把非边缘区域的'O'修改为'X'
           // 边缘区域'O'被改为'E'，还原回'O'
           for (int i = 0; i < m; i++) {
               for (int j = 0; j < n; j++) {
                   if (board[i][j] == 'O') {
                       board[i][j] = 'X';
                   } else if (board[i][j] == 'E') {
                       board[i][j] = 'O';
                   }
               }
           }
       }
   
       void dfs(vector<vector<char>>& board, int i, int j) {
           if (i < 0 || i == board.size() || j < 0 || j == board[0].size() || board[i][j] != 'O') {
               return;
           }
           board[i][j] = 'E'; // 边缘'O'区域标记
           dfs(board, i - 1, j);
           dfs(board, i + 1, j);
           dfs(board, i, j - 1);
           dfs(board, i, j + 1);
       }
   };
   ```

3. [827. 最大人工岛](https://leetcode.cn/problems/making-a-large-island/) (hard)

   > 给你一个大小为 `n x n` 二进制矩阵 `grid` 。**最多** 只能将一格 `0` 变成 `1` 。
   >
   > 返回执行此操作后，`grid` 中最大的岛屿面积是多少？
   >
   > **岛屿** 由一组上、下、左、右四个方向相连的 `1` 形成。
   >
   > **提示：**
   >
   > - `n == grid.length`
   > - `n == grid[i].length`
   > - `1 <= n <= 500`
   > - `grid[i][j]` 为 `0` 或 `1`

   > **思路：** 
   >
   > 1. 用洪水填充，把不相连的岛屿打上不同标记；
   > 2. 再次便利，用数组统计不同的岛屿的大小；
   > 3. 第三次遍历，找为0的点，四周不同相连岛屿岛屿加起来最大面积，用visited数组为每个点上下左右访问过的岛屿编号做标记。

   ```c++
   class Solution {
       vector<vector<int>>* gridptr;
       int n;
       int sign;
   public:
       int largestIsland(vector<vector<int>>& grid) {
           gridptr = &grid;
           n = grid.size();
           // 把不相连的岛屿打上不同标记
           sign = 2;
           for (int i = 0; i < n; i++) {
               for (int j = 0; j < n; j++) {
                   if (grid[i][j] == 1) {
                       dfs(i, j);
                       sign++;
                   }
               }
           }
           // 统计不同的岛屿的大小
           vector<int> landsize(sign, 0);
           int ans = 0; // 记录答案，因为有可能全为1，答案直接为
           for (int i = 0; i < n; i++) {
               for (int j = 0; j < n; j++) {
                   if (grid[i][j] > 0) {
                       ans = max(ans, ++landsize[grid[i][j]]); // 岛屿大小增加，更新答案
                   }
               }
           }
           // 找为0的点，四周不同相连岛屿岛屿加起来最大面积
           vector<bool> visited(sign, false);
           int up, down, left, right, mergeland;
           for (int i = 0; i < n; i++) {
               for (int j = 0; j < n; j++) {
                   if (grid[i][j] == 0) {
                       up = i > 0 ? grid[i - 1][j] : 0; // 上下左右岛屿标号
                       down = i < n - 1 ? grid[i + 1][j] : 0;
                       left = j > 0 ? grid[i][j - 1] : 0;
                       right = j < n - 1 ? grid[i][j + 1] : 0;
                       mergeland = 1 + landsize[up];
                       visited[up] = true; // 合并过的岛屿visited为true
                       if (!visited[down]) {
                           mergeland += landsize[down];
                           visited[down] = true;
                       }
                       if (!visited[left]) {
                           mergeland += landsize[left];
                           visited[left] = true;
                       }
                       if (!visited[right]) {
                           mergeland += landsize[right];
                           visited[right] = true;
                       }
                       ans = max(ans, mergeland);
                       visited[up] = false; // 复原visited数组，便于下一次统计
   					visited[down] = false;
   					visited[left] = false;
   					visited[right] = false;
                   }
               }
           }
           return ans;
       }
   
       void dfs(int i, int j) {
           vector<vector<int>>& grid = *gridptr;
           if (i < 0 || i == n || j < 0 || j == n || grid[i][j] != 1) {
               return;
           }
           grid[i][j] = sign;
           dfs(i - 1, j);
           dfs(i + 1, j);
           dfs(i, j - 1);
           dfs(i, j + 1);
       }
   };
   ```

4. [803. 打砖块](https://leetcode.cn/problems/bricks-falling-when-hit/) (hard)

   > 有一个 `m x n` 的二元网格 `grid` ，其中 `1` 表示砖块，`0` 表示空白。砖块 **稳定**（不会掉落）的前提是：
   >
   > - 一块砖直接连接到网格的顶部，或者
   > - 至少有一块相邻（4 个方向之一）砖块 **稳定** 不会掉落时
   >
   > 给你一个数组 `hits` ，这是需要依次消除砖块的位置。每当消除 `hits[i] = (rowi, coli)` 位置上的砖块时，对应位置的砖块（若存在）会消失，然后其他的砖块可能因为这一消除操作而 **掉落** 。一旦砖块掉落，它会 **立即** 从网格 `grid` 中消失（即，它不会落在其他稳定的砖块上）。
   >
   > 返回一个数组 `result` ，其中 `result[i]` 表示第 `i` 次消除操作对应掉落的砖块数目。
   >
   > **注意**，消除可能指向是没有砖块的空白位置，如果发生这种情况，则没有砖块掉落。
   >
   > **提示：**
   >
   > - `m == grid.length`
   > - `n == grid[i].length`
   > - `1 <= m, n <= 200`
   > - `grid[i][j]` 为 `0` 或 `1`
   > - `1 <= hits.length <= 4 * 104`
   > - `hits[i].length == 2`
   > - `0 <= xi <= m - 1`
   > - `0 <= yi <= n - 1`
   > - 所有 `(xi, yi)` 互不相同

   > **思路：** 
   >
   > 1. 对炮弹击中位置减1，
   > 2. 连接天花板的部分洪水填充，感染为2；
   > 3. 时光倒流，从最后的炮弹开始处理，若这颗炮弹没打，会多出多少砖块，减去命中的砖块本身，得到掉落砖块数量

   ```c++
   class Solution {
       vector<vector<int>>* gridptr;
       int n, m;
   public:
       vector<int> hitBricks(vector<vector<int>>& grid, vector<vector<int>>& hits) {
           gridptr = &grid;
           n = grid.size();
           m = grid[0].size();
           vector<int> ans(hits.size(), 0);
           if (n == 1) { // 高度仅有1行，不会有砖块掉落
               return ans;
           }
           // 炮弹击中位置减1
           for (int k = 0, i, j; k < hits.size(); k++) {
               i = hits[k][0];
               j = hits[k][1];
               grid[i][j]--;
           }
           // 链接天花板的部分洪水填充，感染为2
           for (int j = 0; j < m; j++) {
               if (grid[0][j] == 1) {
                   dfs(0, j);
               }
           }
           // 时光倒流，从最后的炮弹开始处理
           // 这可炮弹没打，会多出多少砖块，减去命中的砖块本身，得到掉落砖块
           for (int k = hits.size() - 1, i, j; k >= 0; k--) {
               i = hits[k][0];
               j = hits[k][1];
               grid[i][j]++; // 把砖块补回来
               if (islink(i, j)) { // 和2相连的1才能感染
                   ans[k] = dfs(i, j) - 1;
               }
           }
           return ans;
       }
   
       int dfs(int i, int j) {
           auto& grid = *gridptr;
           if (i < 0 || i == n || j < 0 || j == m || grid[i][j] != 1) {
               return 0;
           }
           grid[i][j] = 2; // 当前位置感染为2,dfs上下左右，返回新感染的数量
           return 1 + dfs(i - 1, j) + dfs(i + 1, j) + dfs(i, j - 1) + dfs(i, j + 1);
       }
   
       bool islink(int i, int j) {
           auto& grid = *gridptr;
           return grid[i][j] == 1 
           && (i == 0 
           || (i > 0 && grid[i - 1][j] == 2) 
           || (i < n - 1 && grid[i + 1][j] == 2) 
           || (j > 0 && grid[i][j - 1] == 2) 
           || (j < m - 1 && grid[i][j + 1] == 2)); // 和2相连或本身i为0，接着天花板
       }
   };
   ```

   