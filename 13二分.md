# 二分

## 题单

力扣：中等 * 1;	困难 $\times$ 4;

牛客：中等 * 1;

1. [4. 寻找两个正序数组的中位数](https://leetcode.cn/problems/median-of-two-sorted-arrays/)(hard)
2. [875. 爱吃香蕉的珂珂](https://leetcode.cn/problems/koko-eating-bananas/) (medium)
3. [410. 分割数组的最大值](https://leetcode.cn/problems/split-array-largest-sum/) (hard)
4. [机器人跳跃问题](https://www.nowcoder.com/practice/7037a3d57bbd4336856b8e16a9cafd71) (中等)
5. [719. 找出第 K 小的数对距离](https://leetcode.cn/problems/find-k-th-smallest-pair-distance/) (hard)
6. [2141. 同时运行 N 台电脑的最长时间](https://leetcode.cn/problems/maximum-running-time-of-n-computers/) (hard)
7. (谷歌)服务员问题
8. 最快击杀怪兽

## 题目

1. [4. 寻找两个正序数组的中位数](https://leetcode.cn/problems/median-of-two-sorted-arrays/)(hard)

   > 1. 循环时先用 `k` 减去排除掉的数，再更新索引 `i1`, `i2`，因为更新后 `i = tar + 1`,  `tar - i + 1` 值为0.
   > 2. 注意索引不要越界

   ```c++
   class Solution {
   public:
       double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
           int m = nums1.size();
           int n = nums2.size();
           if ((m + n & 1) == 1) {
               // 奇数长度，找第(m+n+1)/2个数
               return kthNum(nums1, nums2, (m + n + 1) / 2, m, n);
           } else {
               // 偶数长度
               return (double)(kthNum(nums1, nums2, (m + n) / 2, m, n) + kthNum(nums1, nums2, (m + n) / 2 + 1, m, n)) / 2;
           }
       }
   
       int kthNum(vector<int>& nums1, vector<int>& nums2, int k, int m, int n) {
           int i1 = 0, i2 = 0;
           while (true) {
               if (i1 == m) { // 数组1下标越界
                   return nums2[i2 + k - 1];
               }
               if (i2 == n) { // 数组2下标越界
                   return nums1[i1 + k - 1];
               }
               if (k == 1) { // 剩下的第1个元素
                   return min(nums1[i1], nums2[i2]);
               }
   
               // 排除前k/2个元素
               int tar1 = min(i1 + k / 2 - 1, m - 1); // 检查边界
               int tar2 = min(i2 + k / 2 - 1, n - 1);
               if (nums1[tar1] <= nums2[tar2]) { // nums1[k/2-1]更小，一定不含第k个元素
                   k -= tar1 - i1 + 1; // 从k中减去排除的个数
                   i1 = tar1 + 1; // 排除nums的前k/2
               } else if (nums1[tar1] > nums2[tar2]) {
                   k -= tar2 - i2 + 1;
                   i2 = tar2 + 1;
               }
           }
       }
   };
   ```

2. [875. 爱吃香蕉的珂珂](https://leetcode.cn/problems/koko-eating-bananas/) (medium)

   > 珂珂喜欢吃香蕉。这里有 `n` 堆香蕉，第 `i` 堆中有 `piles[i]` 根香蕉。警卫已经离开了，将在 `h` 小时后回来。
   >
   > 珂珂可以决定她吃香蕉的速度 `k` （单位：根/小时）。每个小时，她将会选择一堆香蕉，从中吃掉 `k` 根。如果这堆香蕉少于 `k` 根，她将吃掉这堆的所有香蕉，然后这一小时内不会再吃更多的香蕉。  
   >
   > 珂珂喜欢慢慢吃，但仍然想在警卫回来前吃掉所有的香蕉。
   >
   > 返回她可以在 `h` 小时内吃掉所有香蕉的最小速度 `k`（`k` 为整数）。

   > **分析：** 
   >
   > 1. 速度 `k` 减小，耗费小时数会增大，具有单调性；
   > 2. 最终答案范围是1~piles[i]的最大值;

   ```c++
   class Solution {
   public:
       int minEatingSpeed(vector<int>& piles, int h) {
           int l = 1;
           int r = 0;
           for (int& x: piles) {
               r = x > r ? x : r;
           }
           int ans;
           while (l <= r) {
               int m = l + ((r - l) >> 1); // 二分
               if (costh(piles, m) <= h) { // 当前速度满足
                   ans = m;
                   r = m - 1;
               } else { // 当前速度不能满足
                   l = m + 1;
               }
           }
           return ans;
       }
   
       // 以速度pace，全部吃完所需时间
       long long costh(vector<int>& piles, int pace) {
           long long ans = 0;
           for (int& x: piles) {
               ans += (x + pace - 1) / pace; // 向上取整
           }
           return ans;
       }
   };
   ```

3. [410. 分割数组的最大值](https://leetcode.cn/problems/split-array-largest-sum/) (hard)

   > 给定一个非负整数数组 `nums` 和一个整数 `k` ，你需要将这个数组分成 `k` 个非空的连续子数组，使得这 `k` 个子数组各自和的最大值 **最小**。
   >
   > 返回分割后最小的和的最大值。
   >
   > **子数组** 是数组中连续的部分。

   > **画匠问题** 
   >
   > 1. k个画匠画连续画布，完工时间取决于作画时间最长的画匠，划分使得完工时间最短。
   > 2. 二分划分区间数能达标（小于等于 `k` ）的最短完工时间。上界假设只用1个画匠，画完所有的画；下界假设所有的画匠都不用画。
   >
   > **时间复杂度：** $O(nums.size() * \log_2(\sum_{i = 1}^{n}nums[i]))​$ 

   ```c++
   class Solution {
   public:
       int splitArray(vector<int>& nums, int k) {
           // 确定上界
           int sum = 0;
           for (int& num: nums) {
               sum += num;
           }
           int ans = INT_MAX;
           // 二分
           for (int l = 0, r = sum; l <= r;) {
               int m = l + ((r - l) >> 1);
               if (need(nums, m) <= k) { // 能达标
                   ans = m;
                   r = m - 1;
               } else {
                   l = m + 1;
               }
           }
           return ans;
       }
   
       // 当前每组划分最大值为limit，至少划分几组
       int need(vector<int>& nums, int limit) {
           int ans = 1, sum = 0; // 一开始默认至少划分一组
           for (int& n: nums) {
               if (n > limit) {
                   return INT_MAX;
               }
               if (sum + n > limit) { // 加入新元素后超界
                   ans++;
                   sum = n;
               } else {
                   sum += n;
               }
           }
           return ans;
       }
   };
   ```

4. [机器人跳跃问题](https://www.nowcoder.com/practice/7037a3d57bbd4336856b8e16a9cafd71) (中等)

   > 机器人正在玩一个古老的基于DOS的游戏。游戏中有N+1座建筑——从0到N编号，从左到右排列。编号为0的建筑高度为0个单位，编号为i的建筑的高度为H(i)个单位。 
   >
   > 起初， 机器人在编号为0的建筑处。每一步，它跳到下一个（右边）建筑。假设机器人在第k个建筑，且它现在的能量值是E, 下一步它将跳到第个k+1建筑。它将会得到或者失去正比于与H(k+1)与E之差的能量。如果 H(k+1) > E 那么机器人就失去 H(k+1) - E 的能量值，否则它将得到 E - H(k+1) 的能量值。
   >
   > 游戏目标是到达第个N建筑，在这个过程中，能量值不能为负数个单位。现在的问题是机器人以多少能量值开始游戏，才可以保证成功完成游戏？

   ```c++
   #include <iostream>
   #include <array>
   #include <algorithm>
   using namespace std;
   
   enum {
       MAXN = 100001
   };
   static array<int, MAXN> arr = {};
   int bs(int l, int r, int n, int ceil);
   bool game(int e, int n, int ceil);
   
   int main() {
       ios::sync_with_stdio(false);
       cin.tie(nullptr);
       int n;
       while (cin >> n) { // 注意 while 处理多个 case
           int cei = 0;
           for (int i = 1; i <= n; i++) {
               cin >> arr[i];
               cei = max(cei, arr[i]);
           }
           // 二分答案, 0~ceil
           cout << bs(0, cei, n, cei) << '\n';
       }
   }
   
   // 二分答案
   int bs(int l, int r, int n, int cei) {
       int ans;
       while (l <= r) {
           int m = l + ((r - l) >> 1);
           if (game(m, n, cei)) { // 满足
               ans = m;
               r = m - 1;
           } else {
               l = m + 1;
           }
       }
       return ans;
   }
   
   // 初始能量为e，能否满足
   bool game(int e, int n, int ceil) {
       for (int i = 1; i <= n; i++) {
           if (arr[i] > e) {
               e -= arr[i] - e; // 失去能量
           } else {
               e += e - arr[i]; // 得到能量
           }
           if (e >= ceil) { // e的能量已经大于最大高度，接下来数字可能会增长溢出
               return true;
           } 
           if (e < 0) {
               return false;
           }
       }
       return true;
   }
   ```

5. [719. 找出第 K 小的数对距离](https://leetcode.cn/problems/find-k-th-smallest-pair-distance/) (hard)

   > 数对 `(a,b)` 由整数 `a` 和 `b` 组成，其数对距离定义为 `a` 和 `b` 的绝对差值。
   >
   > 给你一个整数数组 `nums` 和一个整数 `k` ，数对由 `nums[i]` 和 `nums[j]` 组成且满足 `0 <= i < j < nums.length` 。返回 **所有数对距离中** 第 `k` 小的数对距离。

   ```c++
   class Solution {
   public:
       int smallestDistancePair(vector<int>& nums, int k) {
           sort(nums.begin(), nums.end()); // 排序，不影响结果，方便滑窗
           // 二分差值小于等于m的数有几队
           int n = nums.size();
           int ans;
           for (int l = 0, r = nums[n - 1] - nums[0], m; l <= r;) {
               m = l + ((r - l) >> 1);
               if (cntp(nums, m) >= k) { // 插值小于等于m对数大于等于k
                   ans = m;
                   r = m - 1;
               } else {
                   l = m + 1;
               }
           }
           return ans;
       }
   
       // 对有序数组找差值小于等于dif的数有几队
       int cntp(vector<int>& nums, int dif) {
           int n = nums.size();
           int cts = 0;
           for (int l = 0, r = 0; l < n; l++) {
               while (r + 1 < n && nums[r + 1] - nums[l] <= dif) {
                   r++;
               }
               cts += r - l;
           }
           return cts;
       }
   };
   ```

6. [2141. 同时运行 N 台电脑的最长时间](https://leetcode.cn/problems/maximum-running-time-of-n-computers/) (hard)

   > 你有 `n` 台电脑。给你整数 `n` 和一个下标从 **0** 开始的整数数组 `batteries` ，其中第 `i` 个电池可以让一台电脑 **运行** `batteries[i]` 分钟。你想使用这些电池让 **全部** `n` 台电脑 **同时** 运行。
   >
   > 一开始，你可以给每台电脑连接 **至多一个电池** 。然后在任意整数时刻，你都可以将一台电脑与它的电池断开连接，并连接另一个电池，你可以进行这个操作 **任意次** 。新连接的电池可以是一个全新的电池，也可以是别的电脑用过的电池。断开连接和连接新的电池不会花费任何时间。
   >
   > 注意，你不能给电池充电。
   >
   > 请你返回你可以让 `n` 台电脑同时运行的 **最长** 分钟数。

   > **思路：** 
   >
   > 1. 想让 `n` 台电脑运行 `t` 分钟，若有电量大于 `t` 的电池，让其单独为一台电脑供电就已经最大程度的利用它了，因为它不可能同时为多台电脑供电；
   > 2. 电量小于等于 `t` 的电池为碎片电池，一定能找到方法，让它为两台电脑供电，时间段不重合而能用光电量。
   > 3. 所以，只要 `碎片电池电量和` 大于等于 `运行时间 * 剩余未供电电脑数`，就能运行 `t` 时间。
   > 4. 剪枝优化，`sum` 大于等于 `最大电池容量 * n`，说明最大运行时间 `t` 大于等于 `最大电池容量` , 所有电池都是碎片电池，最终运行时间为 `sum / n`; 否则，运行时间在 `最大电池容量` 以内。

   ```c++
   class Solution {
   public:
       long long maxRunTime(int n, vector<int>& batteries) {
           long long sum = 0;
           int mb = 0;
           for (int& b: batteries) {
               sum += b;
               mb = max(mb, b);
           }
           // 运行时间大于等于mb, 所有电池都是碎片电池
           // 最终运行时间为sum / n
           if (sum >= (long long)mb * n) {
               return sum / n;
           }
           int l = 0, r = mb, m; // 二分范围在0~mb之间
           int ans = 0;
           while (l <= r) {
               m = l + ((r - l) >> 1);
               // 判断最长运行时间应该在哪个范围
               if (runn(batteries, n, m)) {
                   ans = m;
                   l = m + 1;
               } else {
                   r = m - 1;
               }
           }
           return ans;
       }
   
       bool runn(vector<int>& bs, int num, int t) {
           long long sum = 0;
           for (int& b: bs) {
               if (b > t) { // 完整电池，单独供电
                   num--;
               } else { // 碎片电池，调配供电
                   sum += b;
               }
               if (sum >= (long long)t * num) {
                   return true;
               }
           }
           return false; // 碎片电池加起来不能让剩余电脑跑t分钟
       }
   };
   ```

7. (谷歌)服务员问题

   > 给定一个数组arr长度为n，表示n个服务员，每服务一个人的时间给定一个正数m，表示有m个人等位，如果你是刚来的人，请问你需要等多久？
   > 假设m远远大于n，比如n <= 10^3, m <= 10^9，该怎么做是最优解？

   ```c++
   #include <iostream>
   #include <queue> // 优先队列
   #include <functional> // function
   #include <cstdlib> // rand()
   #include <vector>
   #include <array>
   #include <climits> // INT_MAX
   #include <algorithm> // min()
   #include <ctime> // time()
   
   using namespace std;
   
   // 计算等位时间
   // 给定一个数组arr长度为n，表示n个服务员，每服务一个人的时间
   // 给定一个正数m，表示有m个人等位，如果你是刚来的人，请问你需要等多久？
   // 假设m远远大于n，比如n <= 10^3, m <= 10^9，该怎么做是最优解？
   // 谷歌的面试，这个题连考了2个月
   // 找不到测试链接，所以用对数器验证
   class Solution {
   public:
       // 给定一个正数m，表示有m个人等位，轮到第m+1个人的时间
       // O(m * log(n))
       int waiting1(vector<int>& waiters, int m) {
           // 小根堆
           priority_queue<array<int, 2>, vector<array<int, 2>>, 
           function<bool(const array<int, 2>&, const array<int, 2>&)>> 
           hp([](const array<int, 2>& a, const array<int, 2>& b) {
               return a[0] > b[0];
           });
           int n = waiters.size();
           for (int i = 0; i < n; i++) {
               hp.push(array<int, 2> {0, waiters[i]}); //服务员有空时间段，和服务一个人要花的时间
           }
           // m个人依次被服务，最后堆顶为下一个服务员
           for (int i = 0; i < m; i++) {
               array<int, 2> cur = hp.top();
               hp.pop();
               cur[0] += cur[1];
               hp.push(cur);
           }
           return hp.top()[0];
       };
   
       // 给定一个正数m，表示有m个人等位，轮到第m+1个人的时间
       // O(n + log(m * min_wait))
       int waiting2(vector<int>& waiters, int m) {
           int mint = INT_MAX;
           for (int& num: waiters) {
               mint = min(mint, num);
           }
           // 二分，上界假设全让一个速度最快的服务员服务
           int l = 0, r = mint * m, ans = -1;
           while (l <= r) {
               int mid = l + ((r - l) >> 1);
               // 判断最优答案范围
               if (serint(waiters, mid) >= m + 1) { // m + 1 个人能在mid时间以内等到服务
                   ans = mid;
                   r = mid - 1;
               } else {
                   l = mid + 1;
               }
           }
           return ans;
       }
   private:
       // ti时间能服务几个人
       int serint(vector<int>& waiters, int time) {
           int res = 0;
           for (int& num: waiters) {
               res += time / num + 1;
           }
           return res;
       }
   };
   
   int main() {
       // 对数器验证
       Solution ns;
       srand(time(nullptr));
       int testtimes = 100;
       int N = 50;
       int T = 30;
       int M = 1000;
       for (int turn = 0; turn < testtimes; turn++) {
           int n = rand() % N + 1; // 服务员数量
           vector<int> waiters(n); // 每个服务员服务一个人所需时间
           for (int i = 0; i < n; i++) {
               waiters[i] = rand() % T + 1;
           }
           int m = rand() % M;
           int ans1 = ns.waiting1(waiters, m);
           int ans2 = ns.waiting2(waiters, m);
           if (ans1 != ans2) {
               cout << "Error demo:\n";
               for (int i = 0; i < n; i++) {
                   cout << waiters[i] << ' ';
               }
               cout << '\n';
               cout << "m: " << m << '\n';
               cout << "ans1: " << ans1 << '\n';
               cout << "ans2: " << ans2 << '\n';
           }
       }
       cout << "Test pass\n";
   }
   ```

8. 最快击杀怪兽

   > 直伤,dot最快击杀怪兽问题
   > 怪兽的初始血量是一个整数hp，给出每一回合刀砍和毒杀的数值cuts和poisons
   > 第i回合如果用刀砍，怪兽在这回合会直接损失cuts[i - 1]的血，不再有后续效果
   > 第i回合如果用毒杀，怪兽在这回合不会损失血量，但是之后每回合都损失poisons[i - 1]的血量
   > 并且你选择的所有毒杀效果，在之后的回合都会叠加
   > 两个数组cuts、poisons，长度都是n，代表你一共可以进行n回合
   > 每一回合你只能选择刀砍或者毒杀中的一个动作
   > 如果你在n个回合内没有直接杀死怪兽，意味着你已经无法有新的行动了
   > 但是怪兽如果有中毒效果的话，那么怪兽依然会在血量耗尽的那回合死掉
   > 返回至少多少回合，怪兽会死掉
   > 数据范围 : 
   > 1 <= n <= 10^5
   > 1 <= hp <= 10^9
   > 1 <= cuts[i]、poisons[i] <= 10^9

   ```c++
   #include <iostream>
   #include <vector>
   #include <climits>
   #include <algorithm>
   #include <cstdlib>
   #include <ctime>
   
   using namespace std;
   
   // 直伤,dot最快击杀怪兽问题
   // 怪兽的初始血量是一个整数hp，给出每一回合刀砍和毒杀的数值cuts和poisons
   // 第i回合如果用刀砍，怪兽在这回合会直接损失cuts[i - 1]的血，不再有后续效果
   // 第i回合如果用毒杀，怪兽在这回合不会损失血量，但是之后每回合都损失poisons[i - 1]的血量
   // 并且你选择的所有毒杀效果，在之后的回合都会叠加
   // 两个数组cuts、poisons，长度都是n，代表你一共可以进行n回合
   // 每一回合你只能选择刀砍或者毒杀中的一个动作
   // 如果你在n个回合内没有直接杀死怪兽，意味着你已经无法有新的行动了
   // 但是怪兽如果有中毒效果的话，那么怪兽依然会在血量耗尽的那回合死掉
   // 返回至少多少回合，怪兽会死掉
   // 数据范围 : 
   // 1 <= n <= 10^5
   // 1 <= hp <= 10^9
   // 1 <= cuts[i]、poisons[i] <= 10^9
   // 本题来自真实大厂笔试，找不到测试链接，所以用对数器验证
   class Solution {
   public:
       int turns1(vector<int>& dam, vector<int>& dot, int hp) {
           int sum = 0;
           for (int& num: dot) {
               sum += num;
           }
           vector<vector<vector<int>>> dp  = 
           vector<vector<vector<int>>>(dam.size(), vector<vector<int>>(hp + 1, vector<int>(sum + 1, 0)));
           return f1(dam, dot, 0, hp, 0, dp);
       }
   
       int f1(vector<int>& dam, vector<int>& dot, int i, int r, int p, vector<vector<vector<int>>>& dp) {
           r -= p;
           if (r <= 0) {
               return i + 1;
           }
           if (i == (int)dam.size()) {
               if (p == 0) {
                   return INT_MAX;
               } else {
                   return dam.size() + 1 + (r + p - 1) / p; // 向上取整，dot致死回合数
               }
           }
           if (dp[i][r][p] != 0) {
               return dp[i][r][p];
           }
           int p1 = r <= dam[i] ? (i + 1) : f1(dam, dot, i + 1, r - dam[i], p, dp);
           int p2 = f1(dam, dot, i + 1, r, p + dot[i], dp);
           int ans = min(p1, p2);
           dp[i][r][p] = ans;
           return ans;
       }
   
       int turns2(vector<int>& dam, vector<int>& dot, int hp) {
           // 确定二分上下界, 
           // 每个dam, dot > 0,最慢击杀轮次:前hp-1轮造成1点dam,hp轮造成dot
           int l = 1, r = hp + 1, m;
           int ans = -1;
           while (l <= r) {
               m = l + ((r - l) >> 1);
               if (cal(dam, dot, hp, m)) {
                   ans = m;
                   r = m - 1;
               } else {
                   l = m + 1;
               }
           }
           return ans;
       }
   
   private:
       // t回合能否击杀
       bool cal(vector<int>& dam, vector<int>& dot, int hp, int t) {
           int n = min((int)dam.size(), t);
           for (int i = 0, j = 1; i < n; i++, j++) { // j:当前第几回合
               hp -= max((long long)dam[i], (long long)dot[i] * (t - j));
               if (hp <= 0) {
                   return true;
               }
           }
           return false;
       }
   };
   
   int main() {
       Solution killMonster;
       srand(time(nullptr));
       int testtimes = 100;
       int D = 30;
       int N = 20;
       int HP = 300;
       for (int t = 0; t < testtimes; t++) {
           int n = rand() % N + 1;
           int hp = rand() % HP + 1;
           vector<int> dam(n);
           vector<int> dot(n);
           for (int i = 0; i < n; i++) {
               dam[i] = rand() % D + 1;
               dot[i] = rand() % D + 1;
           }
           int ans1 = killMonster.turns1(dam, dot, hp);
           int ans2 = killMonster.turns2(dam, dot, hp);
           if (ans1 != ans2) {
               cout << 't' << t <<"Wrong:\n";
               cout << "hp: " << hp;
               cout << "\ndam: \n";
               for (int i = 0; i < n; i++) {
                   cout << dam[i] << ' ';
               }
               cout << "\ndot: \n";
               for (int i = 0; i < n; i++) {
                   cout << dot[i] << ' ';
               }
               cout << '\n';
               cout << "ans1: " << ans1 << '\n';
               cout << "ans2: " << ans2 << '\n';
           }
       }
       cout << "End\n";
   }
   ```

   