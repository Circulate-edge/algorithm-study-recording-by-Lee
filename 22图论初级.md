# 图论初级

## 题单

### 1）建图

1. 图的邻接矩阵，邻接表，链式前向星

### 2）拓扑排序

> 力扣：中等 * 1;	困难 * 2;  
>
> 洛谷：普及+/提高− * 1；
>
> 牛客：中等 * 1;  

1. [210. 课程表 II](https://leetcode.cn/problems/course-schedule-ii/) (medium)
2. [【模板】拓扑排序](https://www.nowcoder.com/practice/88f7e156ca7d43a1a535f619cd3f495c) (中等)
3. [U107394](https://www.luogu.com.cn/problem/U107394) (普及+/提高−)
4. [936. 戳印序列](https://leetcode.cn/problems/stamping-the-sequence/) (hard)
5. [LCR 114. 火星词典](https://leetcode.cn/problems/Jf1JuT/) (hard)

### 3）拓扑排序高级应用

> 洛谷：普及 * 1;  
>
> 力扣：中等 * 1;  

1. [P4017最大食物链条数](https://www.luogu.com.cn/problem/P4017) (普及)
2. [851. 喧闹和富有](https://leetcode.cn/problems/loud-and-rich/) (medium)

## 笔记

1. 链式前向星

   > 静态数组实现图的邻接表

2. 拓扑排序

   > 适用有向无环图，使所有点的前置节点遍历顺序在前。
   >
   > * 依次删除入度为0的点，并删除它的影响，及它临界点入度减1，删除的顺序就是拓扑序;
   > * 若中途出现点没有处理完，但没有入度为0的点，说明有环。

## code

### 1）建图

1. 图的邻接矩阵，邻接表，链式前向星

```c++
#include <iostream>
#include <vector>

using namespace std;

// 点的最大数量MAXN, 边的最大数量MAXM
// 只有链式前向星方式建图需要这个数量
// 注意如果无向图的最大数量是m条边，数量要准备m*2
// 因为一条无向边要加两条有向边
enum {MAXN = 11, MAXM = 21};
static vector<vector<int>> matrix(MAXN, vector<int>(MAXN)); // 图的邻接矩阵
static vector<vector<pair<int, int>>> gmap; // 图的邻接表
static vector<int> head(MAXN); // 链式前向星
static vector<int> nextEdge(MAXM);
static vector<int> to(MAXM);
static vector<int> weight(MAXM);
static int cnt;

void build(int n) {
    // 邻接矩阵初始化
    for (int i = 0; i <= n; i++) {
        for (int j = 0; j <= n; j++) {
            matrix[i][j] = 0;
        }
    }
    // 邻接表清空和准备
    gmap.clear();
    gmap.resize(n + 1);
    // 链式前向星头边号数组初始化，边号初始化
    for (int i = 0; i <= n; i++) {
        head[i] = 0;
    }
    cnt = 0;
}

void addEdge(int start, int end, int weighting) {
    // 链式前向行添加新的边，链表头插
    nextEdge[++cnt] = head[start]; // 新边的下一条边指向start点的首边号
    to[cnt] = end;
    weight[cnt] = weighting;
    head[start] = cnt; // start首边号更新为新的边
}

// 三种方式建有向带权图
void directGraph(vector<vector<int>>& edges) {
    for (auto& edge: edges) {
        matrix[edge[0]][edge[1]] = edge[2];
    }
    for (auto& edge: edges) {
        gmap[edge[0]].push_back({edge[1], edge[2]});
    }
    for (auto& edge: edges) {
        addEdge(edge[0], edge[1], edge[2]);
    }
}

// 三种方式建无向带权图
void undirectGraph(vector<vector<int>>& edges) {
    for (auto& edge: edges) {
        matrix[edge[0]][edge[1]] = edge[2];
        matrix[edge[1]][edge[0]] = edge[2];
    }
    for (auto& edge: edges) {
        gmap[edge[0]].push_back({edge[1], edge[2]});
        gmap[edge[1]].push_back({edge[0], edge[2]});
    }
    for (auto& edge: edges) {
        addEdge(edge[0], edge[1], edge[2]);
        addEdge(edge[1], edge[0], edge[2]);
    }
}

void traversal(int n) {
    cout << "adjacenty matrix:\n";
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n; j++) {
            cout << matrix[i][j] << ' ';
        }
        cout << '\n';
    }
    cout << "adjacenty list:\n";
    for (int i = 1; i <=n; i++) {
        cout << i << "(neighbor & weighting): ";
        for (auto& edge: gmap[i]) {
            cout << "(" << edge.first << "," << edge.second << ") ";
        }
        cout << '\n';
    }
    cout << "static adjacenty list:\n";
    for (int i = 1; i <= n; i++) {
        cout << i << "(neighbor & weighting): ";
        for (int ei = head[i]; ei > 0; ei = nextEdge[ei]) { // 下一条边号不为0时继续
            cout << "(" << to[ei] << "," << weight[ei] << ") ";
        }
        cout << '\n';
    }
}

int main() {
    int n1 = 4;
    vector<vector<int>> edges1 = { { 1, 3, 6 }, { 4, 3, 4 }, { 2, 4, 2 }, { 1, 2, 7 }, { 2, 3, 5 }, { 3, 1, 1 } };
    build(n1);
    directGraph(edges1);
    traversal(n1);
    cout << "----------------------\n";
    int n2 = 5;
	vector<vector<int>> edges2 = { { 3, 5, 4 }, { 4, 1, 1 }, { 3, 4, 2 }, { 5, 2, 4 }, { 2, 3, 7 }, { 1, 5, 5 }, { 4, 2, 6 } };
	build(n2);
	undirectGraph(edges2);
	traversal(n2); 
}
```



### 2）拓扑排序

1. [210. 课程表 II](https://leetcode.cn/problems/course-schedule-ii/) (medium)

   > 现在你总共有 `numCourses` 门课需要选，记为 `0` 到 `numCourses - 1`。给你一个数组 `prerequisites` ，其中 `prerequisites[i] = [ai, bi]` ，表示在选修课程 `ai` 前 **必须** 先选修 `bi` 。
   >
   > - 例如，想要学习课程 `0` ，你需要先完成课程 `1` ，我们用一个匹配来表示：`[0,1]` 。
   >
   > 返回你为了学完所有课程所安排的学习顺序。可能会有多个正确的顺序，你只要返回 **任意一种** 就可以了。如果不可能完成所有课程，返回 **一个空数组** 。
   >
   > **提示：** 
   >
   > - `1 <= numCourses <= 2000`
   > - `0 <= prerequisites.length <= numCourses * (numCourses - 1)`
   > - `prerequisites[i].length == 2`
   > - `0 <= ai, bi < numCourses`
   > - `ai != bi`
   > - 所有`[ai, bi]` **互不相同**

   ```c++
   class Solution {
       static const int MAXN = 2001;
       static inline array<int, MAXN> indegree;
   public:
       vector<int> findOrder(int numCourses, vector<vector<int>>& prerequisites) {
           // 图的邻接表
           // 边[ai, bi]的方向是bi->ai;
           vector<vector<int>> linkList(numCourses);
           fill(indegree.begin(), indegree.begin() + numCourses, 0);
           for (auto& edge: prerequisites) {
               linkList[edge[1]].push_back(edge[0]);
               indegree[edge[0]]++; // 入度增加
           }
           vector<int> mqueue(numCourses);
           // 入度为0的点加入队列
           int l = 0, r = 0;
           for (int i = 0; i < numCourses; i++) {
               if (indegree[i] == 0) {
                   mqueue[r++] = i;
               }
           }
           // 依次删除入度为0的点，并处理邻居点，若入度减为0，加入队列
           int cnt = 0;
           while (l < r) {
               int cur = mqueue[l++];
               cnt++;
               for (auto& neighbor: linkList[cur]) {
                   if (--indegree[neighbor] == 0) {
                       mqueue[r++] = neighbor;
                   }
               }
           }
           // 若处理的点数量小于总点数，说明有环
           return cnt == numCourses ? mqueue : vector<int>(0);
       }
   };
   ```

2. [【模板】拓扑排序](https://www.nowcoder.com/practice/88f7e156ca7d43a1a535f619cd3f495c) (中等)

   > **描述** 
   >
   > 给定一个包含*n*个点*m*条边的有向无环图，求出该图的拓扑序。若图的拓扑序不唯一，输出任意合法的拓扑序即可。若该图不能拓扑排序，输出−1−1。
   >
   > **输入描述：** 
   >
   > 第一行输入两个整数n*,*m* ( 1≤n,m≤2⋅1051≤*n*,*m*≤2⋅105)，表示点的个数和边的条数。
   > 接下来的m*行，每行输入两个整数i*u**i*​,*v**i(1≤u,v≤n1≤*u*,*v*≤*n*)，表示*ui*​到vi之间有一条有向边。
   >
   > **输出描述：** 
   >
   > 若图存在拓扑序，输出一行*n*个整数，表示拓扑序。否则输出−1−1。
   > 注意：输出的最后一个数后面不要带空格。

   ```c++
   #include <iostream>
   #include <array>
   #include <algorithm>
   using namespace std;
   
   enum {
       MAXN = 200001, 
       MAXM = 200001
   };
   // 链式前向星
   static array<int, MAXN> headEdge; // 点的头个邻接边号
   static array<int, MAXM> nextEdge; // 该边的下一条边
   static array<int, MAXM> to;
   static int cnt;
   // 拓扑排序，用入度表，队列
   static array<int, MAXN> indegree;
   static array<int, MAXN> myque;
   // static array<int, MAXN> ans;
   void build(int n);
   void addEdge(int a, int b);
   bool topoSort(int n);
   
   int main() {
       int n, m;
       int from, to;
       while (cin >> n >> m) { // n个点m条边
           build(n);
           for (int i = 0; i < m; i++) {
               cin >> from >> to;
               addEdge(from, to);
           }
           if (topoSort(n)) {
               for (int i = 0; i < n - 1; i++) {
                   cout << myque[i] << ' ';
               }
               cout << myque[n - 1] << '\n';
           } else {
               cout << -1 << '\n';
           }
       }
   }
   
   void build(int n) {
       cnt = 0;
       fill(headEdge.begin(), headEdge.begin() + n + 1, 0);
       fill(indegree.begin(), indegree.begin() + n + 1, 0);
   }
   
   void addEdge(int a, int b) {
       // 链表头插
       nextEdge[++cnt] = headEdge[a];
       to[cnt] = b;
       indegree[b]++;
       headEdge[a] = cnt;
   }
   
   bool topoSort(int n) {
       int l = 0, r = 0;
       // 入度为0的点加入队列
       for (int i = 1; i <= n; i++) {
           if (indegree[i] == 0) {
               myque[r++] = i;
           }
       }
       // 依次处理队列列中的点，即删除入度为0的点
       while (l < r) {
           // 遍历点的出边，对邻接点入度减1
           int cur = myque[l++];
           for (int ei = headEdge[cur]; ei != 0; ei = nextEdge[ei]) {
               if (--indegree[to[ei]] == 0) {
                   myque[r++] = to[ei];
               }
           }
       }
       return l == n;
   }
   ```

3. [U107394](https://www.luogu.com.cn/problem/U107394) (普及+/提高−)

   > 字典序最小的拓扑排序
   >
   > **题目描述**
   >
   > 有向无环图上有n个点，m条边。求这张图字典序最小的拓扑排序的结果。字典序最小指希望排好序的结果中，比较靠前的数字尽可能小。
   >
   > **输入格式**
   >
   > 第一行是用空格隔开的两个整数n和m，表示n个点和m条边。
   >
   > 接下来是m行，每行用空格隔开的两个数u和v，表示有一条从u到v的边。
   >
   > **输出格式**
   >
   > 输出一行，拓扑排序的结果，数字之间用空格隔开
   >
   > **说明/提示**
   >
   > $1 \leq n,m \leq 10^5​$ 
   >
   > 注意：图上可能有重边

   > **思路：** 
   >
   > 1. 拓扑排序，用链式前向星建图，加快运行速率；
   > 2. 用小根堆代替队列，实现字典序；

   ```c++
   #include <iostream>
   #include <array>
   #include <algorithm>
   using namespace std;
   
   enum {
       MAXN = 100001,
       MAXM = 100001
   };
   // 链式前向星
   array<int, MAXN> headEdge;
   array<int, MAXM> toNode;
   array<int, MAXM> nextEdge;
   int cnt; // 边号
   // 拓扑排序，入度表
   array<int, MAXN> indegree;
   // 收集结果
   array<int, MAXN> ans;
   // 拓扑排序，小根堆
   array<int, MAXN> heap;
   int heapSize;
   
   void build(int n); // 清空脏数据
   void addEdge(int from, int to); // 链式前向星添加边
   void swap(int i, int j); // 堆的i和j位置交换
   void push(int node); // 小根堆添加
   int pop(); // 小根堆弹出
   bool isEmpty(); // 堆是否为空
   bool toposort(int n); // 拓扑排序
   
   int main(){
       int n, m;
       while (cin >> n >> m) { // n:节点数，m:边数
           build(n);
           // 链式前向星建图，并统计入度
           for (int i = 0, from, to; i < m; i++) {
               cin >> from >> to;
               addEdge(from, to);
               indegree[to]++;
           }
           if (toposort(n)) {
               for (int i = 0; i < n - 1; i++) {
                   cout << ans[i] << ' ';
               }
               cout << ans[n - 1] << '\n';
           }
       }
   }
   
   // 清空脏数据
   void build(int n) {
       cnt = 0;
       heapSize = 0;
       fill(headEdge.begin(), headEdge.begin() + n + 1, 0);
       fill(indegree.begin(), indegree.begin() + n + 1, 0);
   }
   
   // 链式前向星添加边
   void addEdge(int from, int to) {
       nextEdge[++cnt] = headEdge[from]; // 头插
       toNode[cnt] = to;
       headEdge[from] = cnt;
   }
   
   void swap(int i, int j) {
       int tmp = heap[i];
       heap[i] = heap[j];
       heap[j] = tmp;
   }
   
   // 小根堆添加
   void push(int node) {
       int i = heapSize++; // 堆的节点号
       heap[i] = node;
       // 和父节点比较，更小就上浮
       while (heap[i] < heap[(i - 1) / 2]) {
           swap(i, (i - 1) / 2);
           i = (i - 1) / 2; // 跳转到父节点
       }
   }
   
   // 小根堆弹出
   int pop() {
       // 取堆顶元素，堆大小减少
       // 堆底元素换到堆顶，开始下沉
       int num = heap[0];
       heap[0] = heap[--heapSize];
       // 下沉heapify
       int i = 0;
       int l = 1;
       while (l < heapSize) {
           // 左右节点找最小的，然后和父节点比较
           int best = l + 1 < heapSize && heap[l + 1] < heap[l] ? l + 1 : l;
           best = heap[i] > heap[best] ? best : i;
           if (best == i) {
               break;
           }
           // 下沉
           swap(i, best);
           i = best;
           l = 2 * i + 1;
       }
       return num;
   }
   
   bool isEmpty() {
       return heapSize == 0;
   }
   
   bool toposort(int n) {
       // 入度为0的点加入小根堆，删除它
       for (int i = 1; i <= n; i++) {
           if (indegree[i] == 0) {
               push(i);
           }
       }
       int fill = 0; // 处理过的节点数
       // 弹出字典序最小的入度为0节点，
       // 遍历其邻边，减小到达点的入度，若入度为0，再减小
       while (!isEmpty()) {
           int cur = pop();
           ans[fill++] = cur;
           for (int ei = headEdge[cur]; ei != 0; ei = nextEdge[ei]) {
               if (--indegree[toNode[ei]] == 0) {
                   push(toNode[ei]);
               }
           }
       }
       // 最终fill == n,无环
       return fill == n;
   }
   ```

4. [936. 戳印序列](https://leetcode.cn/problems/stamping-the-sequence/) (hard)

   > 你想要用**小写字母**组成一个目标字符串 `target`。 
   >
   > 开始的时候，序列由 `target.length` 个 `'?'` 记号组成。而你有一个小写字母印章 `stamp`。
   >
   > 在每个回合，你可以将印章放在序列上，并将序列中的每个字母替换为印章上的相应字母。你最多可以进行 `10 * target.length`  个回合。
   >
   > 举个例子，如果初始序列为 "?????"，而你的印章 `stamp` 是 `"abc"`，那么在第一回合，你可以得到 "abc??"、"?abc?"、"??abc"。（请注意，印章必须完全包含在序列的边界内才能盖下去。）
   >
   > 如果可以印出序列，那么返回一个数组，该数组由每个回合中被印下的最左边字母的索引组成。如果不能印出序列，就返回一个空数组。
   >
   > 例如，如果序列是 "ababc"，印章是 `"abc"`，那么我们就可以返回与操作 "?????" -> "abc??" -> "ababc" 相对应的答案 `[0, 2]`；
   >
   > 另外，如果可以印出序列，那么需要保证可以在 `10 * target.length` 个回合内完成。任何超过此数字的答案将不被接受。
   >
   > **提示：**
   >
   > 1. `1 <= stamp.length <= target.length <= 1000`
   > 2. `stamp` 和 `target` 只包含小写字母。

   > **思路：** 
   >
   > 1. 从最终目标字符串 `target` 出发，反向思考。
   > 2. 最后一次盖章的位置，一定满足：**它覆盖的所有位置，要么与 target 对应位置相同，要么已经被更早的盖章修正过**。
   > 3. 于是，每个可能的盖章位置（共 `n - m + 1` 个）都可以看作一个“操作”，每个操作有 `m` 个位置需要被“修正”。如果某个位置在盖章后与 `target` 不符，说明这个位置必须被其他操作覆盖（修正），即存在依赖关系。

   ```c++
   class Solution {
   public:
       vector<int> movesToStamp(string stamp, string target) {
           // 拓扑排序，按每次盖章错误的索引数为入度
           int m = stamp.size();
           int n = target.size();
           // 能盖章的点的位置有n - m + 1个
           vector<int> indegree(n - m + 1, m); // 入度表，初始错误的位置有m个
           vector<vector<int>> graph(n); // 图的邻接表
           vector<int> myq(n - m + 1); // 拓扑排序，队列
           int l = 0, r = 0;
           // 若i位置盖章的所有位置正确，则入度为0，加入队列
           // 建图，在i 位置i + j的错误，则i + j到i有一条边
           for (int i = 0; i < n - m + 1; i++) {
               for (int j = 0; j < m; j++) {
                   if (target[i + j] == stamp[j]) {
                       if (--indegree[i] == 0) {
                           myq[r++] = i;
                       }
                   } else {
                       graph[i + j].push_back(i);
                   }
               }
           }
           // 拓扑排序，依次消除入度为0的点,排序的顺序就是可以印出序列的顺序的逆序
           // 已经消除过错误的点不再次消除
           vector<bool> visited(n, false);
           int fill = 0; // 处理的点的数量
           while (l < r) {
               int cur = myq[l++];
               fill++;
               for (int i = 0; i < m; i++) {
                   if (!visited[cur + i]) {
                       visited[cur + i] = true;
                       // 取消cur + i的错误能影响到的开始位置
                       for (auto& idx: graph[cur + i]) {
                           if (--indegree[idx] == 0) {
                               myq[r++] = idx;
                           }
                       }
                   }
               }
           }
           // 若无法完成正确拓扑排序，则没有正确的戳印序列
           if (fill != n - m + 1) {
               return vector<int>();
           }
           for (int i = 0, j = fill - 1; i < j; i++, j--) { // 逆序
               int tmp = myq[i];
               myq[i] = myq[j];
               myq[j] = tmp;
           }
           return myq;
       }
   };
   ```

5. [LCR 114. 火星词典](https://leetcode.cn/problems/Jf1JuT/) (hard)

   > 现有一种使用英语字母的外星文语言，这门语言的字母顺序与英语顺序不同。
   >
   > 给定一个字符串列表 `words` ，作为这门语言的词典，`words` 中的字符串已经 **按这门新语言的字母顺序进行了排序** 。
   >
   > 请你根据该词典还原出此语言中已知的字母顺序，并 **按字母递增顺序** 排列。若不存在合法字母顺序，返回 `""` 。若存在多种可能的合法字母顺序，返回其中 **任意一种** 顺序即可。
   >
   > 字符串 `s` **字典顺序小于** 字符串 `t` 有两种情况：
   >
   > - 在第一个不同字母处，如果 `s` 中的字母在这门外星语言的字母顺序中位于 `t` 中字母之前，那么 `s` 的字典顺序小于 `t` 。
   > - 如果前面 `min(s.length, t.length)` 字母都相同，那么 `s.length < t.length` 时，`s` 的字典顺序也小于 `t` 。
   >
   > **提示：**
   >
   > - `1 <= words.length <= 100`
   > - `1 <= words[i].length <= 100`
   > - `words[i]` 仅由小写英文字母组成

   ```c++
   class Solution {
   public:
       string alienOrder(vector<string>& words) {
           // 初始化入度表，没出现的字母入度为-1，出现过的字母为0
           vector<int> indegree(26, -1); // 入度表
           for (auto& w: words) {
               int m = w.size();
               for (auto& c: w) {
                   indegree[c - 'a'] = 0;
               }
           }
           // 建图，a到b有边表示字典序a在B前
           vector<vector<int>> graph(26);
           int n = words.size();
           for (int i = 0; i < n - 1; i++) {
               string cur = words[i];
               string nxt = words[i + 1];
               int m = min(cur.size(), nxt.size());
               int j = 0;
               for (; j < m; j++) {
                   if (cur[j] != nxt[j]) {
                       graph[cur[j] - 'a'].push_back(nxt[j] - 'a');
                       indegree[nxt[j] - 'a']++;
                       break; // 出现第一个相同索引不同字符，就可以决定单词的字典序
                   }
               }
               if (j < cur.size() && j == nxt.size()) {
                   return "";
               }
           }
           // 将入度为0的点加入队列，统计出现过的字母种类
           vector<int> que(26);
           int l = 0, r = 0, kinds = 26;
           for (int i = 0; i < 26; i++) {
               if (indegree[i] == 0) {
                   que[r++] = i;
               } else if (indegree[i] == -1) {
                   kinds--;
               }
           }
           // 依次删除入度为0的点
           string ans = "";
           while (l < r) {
               int cspot = que[l++];
               ans.push_back(cspot + 'a');
               for (auto& nbor: graph[cspot]) {
                   if (--indegree[nbor] == 0) {
                       que[r++] = nbor;
                   }
               }
           }
           // 无法完成拓扑排序的成环
           return l == kinds ? ans : "";
       }
   };
   ```

   

### 3）拓扑排序高级应用

1. [P4017最大食物链条数](https://www.luogu.com.cn/problem/P4017) (普及)

   > **题目背景** 
   >
   > 你知道食物链吗？Delia 生物考试的时候，数食物链条数的题目全都错了，因为她总是重复数了几条或漏掉了几条。于是她来就来求助你，然而你也不会啊！写一个程序来帮帮她吧。
   >
   > **题目描述** 
   >
   > 给你一个食物网，你要求出这个食物网中最大食物链的数量。
   >
   > （这里的“最大食物链”，指的是**生物学意义上的食物链**，即**最左端是不会捕食其他生物的生产者，最右端是不会被其他生物捕食的消费者**。）
   >
   > Delia 非常急，所以你只有 $1$ 秒的时间。
   >
   > 由于这个结果可能过大，你只需要输出总数模上 $80112002$ 的结果。
   >
   > **输入格式**
   >
   > 第一行，两个正整数 $n$、$m$，表示生物种类 $n$ 和吃与被吃的关系数 $m$。
   >
   > 接下来 $m$ 行，每行两个正整数，表示被吃的生物 A 和吃 A 的生物 B。
   >
   > **输出格式**
   >
   > 一行一个整数，为最大食物链数量模上 $80112002$ 的结果。
   >
   > **说明/提示** 
   >
   > 各测试点满足以下约定：
   >
   > | 测试点编号 |        $n$         |        $m$         |
   > | :--------: | :----------------: | :----------------: |
   > |   $1,2$    |      $\le 40$      |     $\le 400$      |
   > |   $3,4$    |     $\le 100$      | $\le 2\times 10^3$ |
   > |   $5,6$    |     $\le 10^3$     | $\le 6\times 10^4$ |
   > |   $7,8$    | $\le 2\times 10^3$ | $\le 2\times 10^5$ |
   > |   $9,10$   | $\le 5\times 10^3$ | $\le 5\times 10^5$ |
   >
   > 对于 $100\%$ 的数据，$1 \le n \le 5\times 10^3,1\le m \le 5\times 10^5$
   >
   > 【补充说明】
   >
   > 数据中不会出现环，满足生物学的要求。（感谢 @AKEE）

   ```c++
   #include <iostream>
   #include <array>
   #include <algorithm>
   using namespace std;
   
   enum {
       MAXN = 5001,
       MAXM = 500001,
       MOD = 80112002
   };
   // 链式前向星建图
   array<int, MAXN> fstE = {};
   array<int, MAXM> nxt = {};
   array<int, MAXM> destination = {};
   int cnt;
   // 拓扑排序
   array<int, MAXN> indegree = {}; // 入度表
   array<int, MAXN> lines = {}; // 到该点的路线数
   array<int, MAXN> que = {}; // 队列
   void build(int n);
   void addE(int from, int to);
   int toposort(int n);
   
   int main() {
       int n, m;
       cin >> n >> m; // 顶点数n, 边数m
       build(n);
       // 建图，将n跳变加入链式前向星
       for (int i = 0, f, t; i < m; i++) {
           cin >> f >> t;
           addE(f, t);
           indegree[t]++;
       }
       cout << toposort(n) << '\n'; 
   }
   
   // 建图
   void build(int n) {
       fill(indegree.begin(), indegree.begin() + n + 1, 0);
       fill(fstE.begin(), fstE.begin() + n + 1, 0);
       fill(lines.begin(), lines.begin() + n + 1, 0);
       cnt = 0; // 边的编号,有效边从1开始
   }
   // 加边
   void addE(int from, int to) {
       nxt[++cnt] = fstE[from];
       destination[cnt] = to;
       fstE[from] = cnt;
   }
   // 拓扑排序
   int toposort(int n) {
       int l = 0, r = 0;
       // 将入读为0的点加入队列
       // 开始的食物链的路线数为1
       for (int i = 1; i <= n; i++) {
           if (indegree[i] == 0) {
               que[r++] = i;
               lines[i] = 1;
           }
       }
       // 依次处理入度为0的点
       // 最顶端消费者统计边的数量
       int ans = 0;
       while (l < r) {
           int cur = que[l++];
           if (fstE[cur] == 0) {
               ans = (ans + lines[cur]) % MOD;
           }
           // 遍历邻接边，处理邻点
           for (int ei = fstE[cur], v; ei != 0; ei = nxt[ei]) {
               v = destination[ei]; // 邻点
               lines[v] = (lines[v] + lines[cur]) % MOD;
               if (--indegree[v] == 0) {
                   que[r++] = v;
               }
           }
       }
       return ans;
   }
   ```

2. [851. 喧闹和富有](https://leetcode.cn/problems/loud-and-rich/) (medium)

   > 有一组 `n` 个人作为实验对象，从 `0` 到 `n - 1` 编号，其中每个人都有不同数目的钱，以及不同程度的安静值（quietness）。为了方便起见，我们将编号为 `x` 的人简称为 "person `x` "。
   >
   > 给你一个数组 `richer` ，其中 `richer[i] = [ai, bi]` 表示 person `ai` 比 person `bi` 更有钱。另给你一个整数数组 `quiet` ，其中 `quiet[i]` 是 person `i` 的安静值。`richer` 中所给出的数据 **逻辑自洽**（也就是说，在 person `x` 比 person `y` 更有钱的同时，不会出现 person `y` 比 person `x` 更有钱的情况 ）。
   >
   > 现在，返回一个整数数组 `answer` 作为答案，其中 `answer[x] = y` 的前提是，在所有拥有的钱肯定不少于 person `x` 的人中，person `y` 是最不安静的人（也就是安静值 `quiet[y]` 最小的人）。
   >
   > **提示：**
   >
   > - `n == quiet.length`
   > - `1 <= n <= 500`
   > - `0 <= quiet[i] < n`
   > - `quiet` 的所有值 **互不相同**
   > - `0 <= richer.length <= n * (n - 1) / 2`
   > - `0 <= ai, bi < n`
   > - `ai != bi`
   > - `richer` 中的所有数对 **互不相同**
   > - 对 `richer` 的观察在逻辑上是一致的

   > **思路：** 
   >
   > 1. 拓扑排序，建图，a比b更有钱则加一条a到b的边；
   > 2. 传递ans的信息，a比b有钱，ans[a]也比b有钱，且ans[a]的quiet值是**到a的来源**中最小的

   ```c++
   class Solution {
   public:
       vector<int> loudAndRich(vector<vector<int>>& richer, vector<int>& quiet) {
           // 拓扑排序，建图，a比b更有钱则加一条a到b的边
           int n = quiet.size();
           vector<vector<int>> graph(n);
           vector<int> indegree(n, 0);
           for (auto& e: richer) {
               graph[e[0]].push_back(e[1]);
               indegree[e[1]]++;
           }
           // 初始化答案，钱不少于i的最喧嚣的人初始设为自己
           // 拓扑排序，入度为0的点加入队列
           vector<int> ans(n);
           vector<int> que(n);
           int l = 0, r = 0;
           for (int i = 0; i < n; i++) {
               ans[i] = i;
               if (indegree[i] == 0) {
                   que[r++] = i;
               }
           }
           // 拓扑排序，顺序删除入度为0的点
           // cur->nxt传递ans[]的信息，a比b有钱，ans[a]也比b有钱，且ans[a]的quiet值是到a的来源中最小的
           // 若ans[cur]比ans[nxt]的quiet值更低，则更新ans[nxt]为ans[cur]
           while (l < r) {
               int cur = que[l++];
               for (auto& nxt: graph[cur]) {
                   if (--indegree[nxt] == 0) {
                       que[r++] = nxt;
                   }
                   if (quiet[ans[cur]] < quiet[ans[nxt]]) {
                       ans[nxt] = ans[cur];
                   }
               }
           }
           return ans;
       }
   };
   ```

   