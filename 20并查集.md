# 20并查集

## 题单

> 力扣：中等 * 2;	困难 * 5;  
>
> 洛谷：中等 * 1;  
>
> 牛客：简单 * 1;  

1. [并查集的实现](https://www.nowcoder.com/practice/e7ed657974934a30b2010046536a5372) (简单)
2. [P3367     【模板】并查集   ](https://www.luogu.com.cn/problem/P3367) (普及)
3. [765. 情侣牵手](https://leetcode.cn/problems/couples-holding-hands/) (hard)
4. [839. 相似字符串组](https://leetcode.cn/problems/similar-string-groups/) (hard)
5. [200. 岛屿数量](https://leetcode.cn/problems/number-of-islands/) (medium)
6. [947. 移除最多的同行或同列石头](https://leetcode.cn/problems/most-stones-removed-with-same-row-or-column/) (medium)
7. [2092. 找出知晓秘密的所有专家](https://leetcode.cn/problems/find-all-people-with-secret/)  (hard)
8. [2421. 好路径的数目](https://leetcode.cn/problems/number-of-good-paths/) (hard)
9. [928. 尽量减少恶意软件的传播 II](https://leetcode.cn/problems/minimize-malware-spread-ii/) (hard)

## code

1. [并查集的实现](https://www.nowcoder.com/practice/e7ed657974934a30b2010046536a5372) (简单)

   > 给定一个没有重复值的整形数组arr，初始时认为arr中每一个数各自都是一个单独的集合。请设计一种叫`UnionFind` 的结构，并提供以下两个操作。
   >
   > 1. `boolean isSameSet(int a, int b)`: 查询a和b这两个数是否属于一个集合
   > 2. `void union(int a, int b)`: 把a所在的集合与b所在的集合合并在一起，原本两个集合各自的元素以后都算作同一个集合
   >
   > [要求]
   >
   > 如果调用`isSameSet` 和 `union` 的总次数逼近或超过 `O(N)` ，请做到单次调用 `isSameSet` 或 `union` 方法的平均时间复杂度为 `O(1)` 
   >
   > **输入描述：** 
   >
   > 第一行两个整数N, M。分别表示数组大小、操作次数
   > 接下来M行，每行有一个整数opt
   > 若opt = 1，后面有两个数x, y，表示查询(x, y)这两个数是否属于同一个集合
   > 若opt = 2，后面有两个数x, y，表示把x, y所在的集合合并在一起
   >
   > **输出描述：** 
   >
   > 对于每个opt = 1的操作，若为真则输出"Yes"，否则输出"No"

   ```c++
   #include <iostream>
   #include <array>
   
   using namespace std;
   
   enum {
       MAXN = 1000001
   };
   static array<int, MAXN> father;
   static array<int, MAXN> setsize;
   static array<int, MAXN> stk;
   void build(int n);
   int find(int node);
   bool isSameSet(int a, int b);
   void unite(int a, int b);
   
   int main() {
       int n, m;
       int opt, a, b;
       while (cin >> n >> m) { // 数组大小n, 操作次数m
           build(n);
           for (int i = 0; i < m; i++) {
               cin >> opt >> a >> b;
               if (opt == 1) {
                   cout << (isSameSet(a, b) ? "Yes\n" : "No\n");
               } else if (opt == 2) {
                   unite(a, b);
               }
           }
       }
   }
   
   void build(int n) {
       for (int i = 0; i < n; i++) {
           father[i] = i;
           setsize[i] = 1;
       }
   }
   
   int find(int node) {
       int size = 0;
       // 跳转父节点，记录沿途节点，找到源头
       while (node != father[node]) {
           stk[size++] = node;
           node = father[node];
       }
       // 扁平化，将沿途节点指向源头
       while (size > 0) {
           father[stk[--size]] = node;
       }
       return node;
   }
   
   bool isSameSet(int a, int b) {
       return find(a) == find(b);
   }
   
   void unite(int a, int b) {
       int ra = find(a);
       int rb = find(b);
       // 小的集合挂到大的上
       if (ra != rb) {
           if (setsize[ra] >= setsize[rb]) {
               setsize[ra] += setsize[rb]; 
               father[rb] = ra;
           } else {
               setsize[rb] += setsize[rb];
               father[ra] = rb;
           }
       }
   }
   ```

2. [P3367     【模板】并查集   ](https://www.luogu.com.cn/problem/P3367) (普及)

   > **题目描述** 
   >
   > 如题，现在有一个并查集，你需要完成合并和查询操作。
   >
   > **输入格式** 
   >
   > 第一行包含两个整数 $N,M$ ,表示共有 $N$ 个元素和 $M$ 个操作。
   >
   > 接下来 $M$ 行，每行包含三个整数 $Z_i,X_i,Y_i$ 。
   >
   > 当 $Z_i=1$ 时，将 $X_i$ 与 $Y_i$ 所在的集合合并。
   >
   > 当 $Z_i=2$ 时，输出 $X_i$ 与 $Y_i$ 是否在同一集合内，是的输出 
   >  `Y` ；否则输出 `N` 。
   >
   > **输出格式** 
   >
   > 对于每一个 $Z_i=2$ 的操作，都有一行输出，每行包含一个大写字母，为 `Y` 或者 `N` 。
   >
   > **说明/提示** 
   >
   > 对于 $15\%$ 的数据，$N \le 10$，$M \le 20$。
   >
   > 对于 $35\%$ 的数据，$N \le 100$，$M \le 10^3$。
   >
   > 对于 $50\%$ 的数据，$1\le N \le 10^4$，$1\le M \le 2\times 10^5$。
   >
   > 对于 $100\%$ 的数据，$1\le N\le 2\times 10^5$，$1\le M\le 10^6$，$1 \le X_i, Y_i \le N$，$Z_i \in \{ 1, 2 \}$。

   ```c++
   #include <iostream>
   #include <array>
   
   using namespace std;
   enum {MAXN = 200001};
   static array<int, MAXN> father;
   void build(int n);
   int find(int node);
   bool isSameSet(int a, int b);
   void unite(int a, int b);
   
   int main() {
       int n, m;
       int opt, a, b;
       while (cin >> n >> m) { // 数组大小n, 操作次数m
           build(n);
           for (int i = 0; i < m; i++) {
               cin >> opt >> a >> b;
               if (opt == 1) { // 合并
                   unite(a, b);
               } else if (opt == 2) { // 查找是否同一集合
                   cout << (isSameSet(a, b) ? "Y\n" : "N\n");
               }
           }
       }
   }
   
   void build(int n) {
       // 初始指向自己
       for (int i = 0; i < n; i++) {
           father[i] = i;
       }
   }
   
   int find(int node) {
       // 非源头节点扁平化调整
       if (node != father[node]) {
           father[node] = find(father[node]);
       }
       return father[node]; // 调整完成后这一支父节点都指向源头
   }
   
   bool isSameSet(int a, int b) {
       return find(a) == find(b);
   }
   
   void unite(int a, int b) {
       father[find(a)] = find(b);
   }
   ```

3. [765. 情侣牵手](https://leetcode.cn/problems/couples-holding-hands/) (hard)

   > `n` 对情侣坐在连续排列的 `2n` 个座位上，想要牵到对方的手。
   >
   > 人和座位由一个整数数组 `row` 表示，其中 `row[i]` 是坐在第 `i `个座位上的人的 **ID**。情侣们按顺序编号，第一对是 `(0, 1)`，第二对是 `(2, 3)`，以此类推，最后一对是 `(2n - 2, 2n - 1)`。
   >
   > 返回 *最少交换座位的次数，以便每对情侣可以并肩坐在一起*。 *每次*交换可选择任意两人，让他们站起来交换座位。
   >
   > **提示:**
   >
   > - `2n == row.length`
   > - `2 <= n <= 30`
   > - `n` 是偶数
   > - `0 <= row[i] < 2n`
   > - `row` 中所有元素均 **无重复** 

   ```c++
   class Solution {
       static const int MAXN = 60;
       inline static array<int, MAXN> father;
       int setnum;
   public:
       int minSwapsCouples(vector<int>& row) {
           // 建立并查集，初始数量为情侣对数，若情侣编号不同，则合并
           // 每个集合情侣对数减1，就是集合中要交换几次
           // 总交换次数为 `情侣对数-剩余集合数` 
           int n = row.size();
           setnum = n / 2;
           build(n / 2);
           for (int i = 0; i < n; i += 2) {
               merge(row[i] / 2, row[i + 1] / 2); // 检查情侣编号
           }
           return n / 2 - setnum;
       }
   
       void build(int m) {
           for (int i = 0; i < m; i++) {
               father[i] = i;
           }
       }
   
       int find(int node) {
           // 递归找根源节点，过程中扁平化调整
           if (node != father[node]) {
               father[node] = find(father[node]);
           }
           return father[node];
       }
   
       void merge(int a, int b) {
           // 不是一对情侣，合并情侣编号集合，集合数减少
           int ra = find(a);
           int rb = find(b);
           if (ra != rb) {
               father[ra] = rb;
               setnum--;
           }
       }
   };
   ```

4. [839. 相似字符串组](https://leetcode.cn/problems/similar-string-groups/) (hard)

   > 如果交换字符串 `X` 中的两个不同位置的字母，使得它和字符串 `Y` 相等，那么称 `X` 和 `Y` 两个字符串相似。如果这两个字符串本身是相等的，那它们也是相似的。
   >
   > 例如，`"tars"` 和 `"rats"` 是相似的 (交换 `0` 与 `2` 的位置)； `"rats"` 和 `"arts"` 也是相似的，但是 `"star"` 不与 `"tars"`，`"rats"`，或 `"arts"` 相似。
   >
   > 总之，它们通过相似性形成了两个关联组：`{"tars", "rats", "arts"}` 和 `{"star"}`。注意，`"tars"` 和 `"arts"` 是在同一组中，即使它们并不相似。形式上，对每个组而言，要确定一个单词在组中，只需要这个词和该组中至少一个单词相似。
   >
   > 给你一个字符串列表 `strs`。列表中的每个字符串都是 `strs` 中其它所有字符串的一个字母异位词。请问 `strs` 中有多少个相似字符串组？
   >
   > **提示：** 
   >
   > - `1 <= strs.length <= 300` 
   > - `1 <= strs[i].length <= 300` 
   > - `strs[i]` 只包含小写字母。
   > - `strs` 中的所有单词都具有相同的长度，且是彼此的字母异位词。

   ```c++
   class Solution {
       static const int MAXN = 301;
       static inline array<int, MAXN> father = {};
       int setnum;
   public:
       int numSimilarGroups(vector<string>& strs) {
           int n = strs.size();
           int ssize = strs[0].size();
           build(n);
           for (int i = 0; i < n; i++) {
               for (int j = i + 1; j < n; j++) {
                   // 不在一个集合，检查是否为相似, 相似合并集合编号
                   if (find(i) != find(j)) {
                       int dif = 0;
                       for (int k = 0; k < ssize && dif < 3; k++) {
                           if (strs[i][k] != strs[j][k]) {
                               dif++;
                           }
                       }
                       if (dif == 0 || dif == 2) {
                           merge(i, j);
                       }
                   }
               }
           }
           return setnum;
       }
   
       // n个集合
       void build(int n) {
           for (int i = 0; i < n; i++) {
               father[i] = i;
               setnum = n;
           }
       }
   
       int find(int node) {
           // 扁平化，递归父节点都指向根源
           if (node != father[node]) {
               father[node] = find(father[node]);
           }
           return father[node]; // 往上找根源
       }
   
       // 合并集合，总集合数减少
       void merge(int a, int b) {
           int ra = find(a);
           int rb = find(b);
           if (ra != rb) {
               father[ra] = rb;
               setnum--;
           }
       }
   };
   ```

5. [200. 岛屿数量](https://leetcode.cn/problems/number-of-islands/) (medium)

   > 给你一个由 `'1'`（陆地）和 `'0'`（水）组成的的二维网格，请你计算网格中岛屿的数量。
   >
   > 岛屿总是被水包围，并且每座岛屿只能由水平方向和/或竖直方向上相邻的陆地连接形成。
   >
   > 此外，你可以假设该网格的四条边均被水包围。

   > **思路：** 
   >
   > 1. 使用并查集，初始集合数量为所有陆地，
   > 2. 检查陆地的左侧和右侧，若也是陆地，就合并集合，并减少集合数量

   ```c++
   class Solution {
       static const int MAXN = 300 * 300;
       static inline array<int, MAXN> father = {};
       int row, col, setnum;
   public:
       int numIslands(vector<vector<char>>& grid) {
           row = grid.size();
           col = grid[0].size();
           build(grid);
           for (int i = 0; i < row; i++) {
               for (int j = 0; j < col; j++) {
                   if (grid[i][j] == '1') { // 当前是陆地
                       if (i > 0 && grid[i - 1][j] == '1') { // 上方是陆地连起来
                           merge(i, j, i - 1, j);
                       }
                       if (j > 0 && grid[i][j - 1] == '1') { // 左侧是陆地，连起来
                           merge(i, j, i, j - 1);
                       }
                   }
               }
           }
           return setnum;
       }
   
       // 初始化集合，数量为陆地数量
       void build(vector<vector<char>>& grid) {
           setnum = 0;
           for (int i = 0; i < row; i++) {
               for (int j = 0; j < col; j++) {
                   if (grid[i][j] == '1') {
                       int idx = index(i, j);
                       father[idx] = idx;
                       setnum++;
                   }
               }
           }
       }
   
       // 查找集合索引
       int index(int i, int j) {
           return i * col + j;
       }
   
       int find(int idx) {
           // 递归根源，使过程节点指向根源节点
           if (idx != father[idx]) {
               father[idx] = find(father[idx]);
           }
           return father[idx];
       }
   
       // 合并集合
       void merge(int a, int b, int c, int d) {
           int ri = find(index(a, b));
           int rj = find(index(c, d));
           if (ri != rj) {
               father[rj] = ri;
               setnum--;
           }
       }
   };
   ```

6. [947. 移除最多的同行或同列石头](https://leetcode.cn/problems/most-stones-removed-with-same-row-or-column/) (medium)

   > `n` 块石头放置在二维平面中的一些整数坐标点上。每个坐标点上最多只能有一块石头。
   >
   > 如果一块石头的 **同行或者同列** 上有其他石头存在，那么就可以移除这块石头。
   >
   > 给你一个长度为 `n` 的数组 `stones` ，其中 `stones[i] = [xi, yi]` 表示第 `i` 块石头的位置，返回 **可以移除的石子** 的最大数量。
   >
   > **提示：** 
   >
   > - `1 <= stones.length <= 1000` 
   > - `0 <= xi, yi <= 104` 
   > - 不会有两块石头放在同一个坐标点上

   > **思路：** 
   >
   > 1. 用哈希表记录每行和每列第一个陆地编号
   > 2. 后续出现有和之前同行或同列的，合并集合
   > 3. 每个集合的石子数减1，就是这个集合可以移除的石子数
   > 4. 总的可移除石子数，就是石子数 `n` 减去集合数 `setnum` 

   ```c++
   class Solution {
       static const int MAXN = 1001;
       unordered_map<int, int> rowmap;
       unordered_map<int, int> colmap;
       static inline array<int, MAXN> father;
       int setnum;
   public:
       int removeStones(vector<vector<int>>& stones) {
           int n = stones.size();
           build(n);
           // 用哈希表记录每行和每列第一个石子编号
           // 后续出现有和之前同行或同列的，合并集合
           for (int i = 0; i < n; i++) {
               int x = stones[i][0];
               int y = stones[i][1];
               if (rowmap.count(x) == 1) {
                   merge(rowmap[x], i);
               } else {
                   rowmap.insert({x, i});
               }
               if (colmap.count(y) == 1) {
                   merge(colmap[y], i);
               } else {
                   colmap.insert({y, i});
               }
           }
           return n - setnum;
       }
   
       void build(int n) {
           rowmap.clear();
           colmap.clear();
           for (int i = 0; i < n; i++) {
               father[i] = i;
           }
           setnum = n;
       }
   
       int find(int node) {
           if (node != father[node]) {
               father[node] = find(father[node]);
           }
           return father[node];
       }
   
       void merge(int a, int b) {
           int ra = find(a);
           int rb = find(b);
           if (ra != rb) {
               father[ra] = rb;
               setnum--;
           }
       }
   };
   ```

7. [2092. 找出知晓秘密的所有专家](https://leetcode.cn/problems/find-all-people-with-secret/)  (hard)

   > 给你一个整数 `n` ，表示有 `n` 个专家从 `0` 到 `n - 1` 编号。另外给你一个下标从 0 开始的二维整数数组 `meetings` ，其中 `meetings[i] = [xi, yi, timei]` 表示专家 `xi` 和专家 `yi` 在时间 `timei` 要开一场会。一个专家可以同时参加 **多场会议** 。最后，给你一个整数 `firstPerson` 。
   >
   > 专家 `0` 有一个 **秘密** ，最初，他在时间 `0` 将这个秘密分享给了专家 `firstPerson` 。接着，这个秘密会在每次有知晓这个秘密的专家参加会议时进行传播。更正式的表达是，每次会议，如果专家 `xi` 在时间 `timei` 时知晓这个秘密，那么他将会与专家 `yi` 分享这个秘密，反之亦然。
   >
   > 秘密共享是 **瞬时发生** 的。也就是说，在同一时间，一个专家不光可以接收到秘密，还能在其他会议上与其他专家分享。
   >
   > 在所有会议都结束之后，返回所有知晓这个秘密的专家列表。你可以按 **任何顺序** 返回答案。
   >
   > **提示：** 
   >
   > - `2 <= n <= 105` 
   > - `1 <= meetings.length <= 105` 
   > - `meetings[i].length == 3` 
   > - `0 <= xi, yi <= n - 1` 
   > - `xi != yi` 
   > - `1 <= timei <= 105` 
   > - `1 <= firstPerson <= n - 1` 

   > **思路：** 
   >
   > 1. 使用并查集，按开会时段顺序，合并同一时段一起开会的专家，若其中有知道秘密的，那同时开会的专家集团都知道秘密了；
   > 2. 开会结束，撤销不知道秘密的专家集团，避免后续时段其中有人知道秘密，被错误判断为这整个集团都知道秘密；

   ```c++
   class Solution {
       static const int MAXN = 100001;
       static inline array<int, MAXN> father;
       static inline array<bool, MAXN> secret;
   public:
       vector<int> findAllPeople(int n, vector<vector<int>>& meetings, int firstPerson) {
           // 按时间排序数组
           int m = meetings.size();
           sort(meetings.begin(), meetings.end(), [](const vector<int>& a, const vector<int>& b) {
               return a[2] < b[2];
           });
           // 同一时段的专家开会
           build(n, firstPerson);
           for (int l = 0, r = 0; l < m;) {
               while (r + 1 < m && meetings[l][2] == meetings[r + 1][2]) {
                   r++;
               }
               // 在一起开会的专家传递秘密
               for (int i = l; i <= r; i++) {
                   merge(meetings[i][0], meetings[i][1]);
               }
               // 开完会后撤销不知道秘密的并查集
               // 因为后续时段不在一起
               for (int i = l, a, b; i <= r; i++) {
                   a = meetings[i][0];
                   b = meetings[i][1];
                   if (!secret[find(a)]) {
                       father[a] = a;
                   }
                   if (!secret[find(b)]) {
                       father[b] = b;
                   }
               }
               l = r + 1;
           }
           vector<int> ans(0);
           for (int i = 0; i < n; i++) {
               if (secret[find(i)]) {
                   ans.push_back(i);
               }
           }
           return ans;
       }
   
       void build(int n, int firstPerson) {
           for (int i = 0; i < n; i++) {
               father[i] = i;
               secret[i] = false;
           }
           secret[0] = true;
           father[firstPerson] = 0;
       }
   
       int find(int i) {
           if (i != father[i]) {
               father[i] = find(father[i]);
           }
           return father[i];
       }
   
       // 合并，若有知晓秘密的集团则交换秘密
       void merge(int a, int b) {
           int ra = find(a);
           int rb = find(b);
           if (ra != rb) {
               father[ra] = rb;
               secret[rb] |= secret[ra];
           }
       }
   };
   ```

8. [2421. 好路径的数目](https://leetcode.cn/problems/number-of-good-paths/) (hard)

   > 给你一棵 `n` 个节点的树（连通无向无环的图），节点编号从 `0` 到 `n - 1` 且恰好有 `n - 1` 条边。
   >
   > 给你一个长度为 `n` 下标从 **0** 开始的整数数组 `vals` ，分别表示每个节点的值。同时给你一个二维整数数组 `edges` ，其中 `edges[i] = [ai, bi]` 表示节点 `ai` 和 `bi` 之间有一条 **无向** 边。
   >
   > 一条 **好路径** 需要满足以下条件：
   >
   > 1. 开始节点和结束节点的值 **相同** 。
   > 2. 开始节点和结束节点中间的所有节点值都 **小于等于** 开始节点的值（也就是说开始节点的值应该是路径上所有节点的最大值）。
   >
   > 请你返回不同好路径的数目。
   >
   > 注意，一条路径和它反向的路径算作 **同一** 路径。比方说， `0 -> 1` 与 `1 -> 0` 视为同一条路径。单个节点也视为一条合法路径。
   >
   > **提示：** 
   >
   > - `n == vals.length` 
   > - `1 <= n <= 3 * 104` 
   > - `0 <= vals[i] <= 105` 
   > - `edges.length == n - 1` 
   > - `edges[i].length == 2` 
   > - `0 <= ai, bi < n` 
   > - `ai != bi` 
   > - `edges` 表示一棵合法的树。

   > **思路：** 
   >
   > 1. 对边进行升序排序，按端点最大值；按节点值从小到大处理边，可以保证任意2个值为 `val` 的节点连通时，路径中不含大于 `val` 的节点。
   >
   > 2. 顺序用并查集合并边集中边的端点，合并后要知道该集团的最大顶点，及最大值相等个数；
   >
   > 3. 和并的2个节点集合最大值相等时，说明出现了好路径，数量为 
   >
   >    ```
   >    maxcnts[find(a)] * maxcnts[find(b)]
   >    ```
   >
   >    ，即分别以 `a` 集团各最大值节点作一个端点，分别以 `b` 集团中各最大值节点作另一端点的路径数目，返回新增的好路径数目。

   ```c++
   class Solution {
       static const int MAXN = 30001;
       static inline array<int, MAXN> father = {}; // 父节点索引
       static inline array<int, MAXN> maxcnts = {}; // 集团最大值个数
   public:
       int numberOfGoodPaths(vector<int>& vals, vector<vector<int>>& edges) {
           // 对边按端点最大值升序排序
           sort(edges.begin(), edges.end(), [&vals](const vector<int>& a, const vector<int>& b) {
               return max(vals[a[0]], vals[a[1]]) < max(vals[b[0]], vals[b[1]]);
           });
           int n = vals.size();
           int m = edges.size();
           build(n);
           int ans = n; // 节点自己是一条好路径
           for (int i = 0; i < m; i++) {
               ans += merge(vals, edges[i][0], edges[i][1]);
           }
           return ans;
       }
   
       void build(int n) {
           for (int i = 0; i < n; i++) {
               father[i] = i;
               maxcnts[i] = 1;
           }
       }
   
       int find(int i) {
           // 递归找代表节点，并是结构扁平化
           if (i != father[i]) {
               father[i] = find(father[i]);
           }
           return father[i];
       }
   
       int merge(vector<int>& vals, int a, int b) {
           // 返回处理本条边带来好路径数目
           int ra = find(a);
           int rb = find(b);
           int path = 0;
           // 让端点值大的做代表节点
           // 本题处理的是树，不会出现环或2个节点间有多条通路
           if (vals[ra] < vals[rb]) {
               father[ra] = rb;
           } else if (vals[ra] > vals[rb]) {
               father[rb] = ra;
           } else {
               path += maxcnts[ra] * maxcnts[rb];
               maxcnts[rb] += maxcnts[ra];
               father[ra] = rb;
           }
           return path;
       }
   };
   ```

9. [928. 尽量减少恶意软件的传播 II](https://leetcode.cn/problems/minimize-malware-spread-ii/) (hard)

   > 给定一个由 `n` 个节点组成的网络，用 `n x n` 个邻接矩阵 `graph` 表示。在节点网络中，只有当 `graph[i][j] = 1` 时，节点 `i` 能够直接连接到另一个节点 `j`。
   >
   > 一些节点 `initial` 最初被恶意软件感染。只要两个节点直接连接，且其中至少一个节点受到恶意软件的感染，那么两个节点都将被恶意软件感染。这种恶意软件的传播将继续，直到没有更多的节点可以被这种方式感染。
   >
   > 假设 `M(initial)` 是在恶意软件停止传播之后，整个网络中感染恶意软件的最终节点数。
   >
   > 我们可以从 `initial` 中 **删除一个节点**，**并完全移除该节点以及从该节点到任何其他节点的任何连接。**
   >
   > 请返回移除后能够使 `M(initial)` 最小化的节点。如果有多个节点满足条件，返回索引 **最小的节点** 。
   >
   > **提示：** 
   >
   > * `n == graph.length` 
   > * `n == graph[i].length` 
   > * `2 <= n <= 300` 
   > * `graph[i][j]` 是 `0` 或 `1`.
   > * `graph[i][j] == graph[j][i]` 
   > * `graph[i][i] == 1` 
   > * `1 <= initial.length < n` 
   > * `0 <= initial[i] <= n - 1` 
   > *  `initial` 中每个整数都**不同** 

   > **思路：** 
   >
   > 1. 合并非感染源普通节点集团，
   > 2. 遍历感染源, 感染邻居节点，被多次感染的集团代表节点标记为 `-2`, 表示无效，
   > 3. 遍历代表节点，为统计其感染源节点的单独感染数，
   > 4. 最后在感染源节点中找出单独感染最多的，因为若相同，返回下标小的，所以先对感染源节点按下标排序。

   ```c++
   class Solution {
       static const int MAXN = 301;
       static inline array<int, MAXN> father = {}; // 并查集父节点
       static inline array<int, MAXN> setsize = {}; // 并查集大小
       static inline array<bool, MAXN> virus = {}; // 是否携带病毒
       static inline array<int, MAXN> infect = {}; // 感染源头节点
       static inline array<int, MAXN> cnts = {}; // 病毒节点单独感染数
       const vector<int>* initPtr;
   public:
       int minMalwareSpread(vector<vector<int>>& graph, vector<int>& initial) {
           initPtr = &initial;
           int n = graph.size();
           build(n);
           // 合并非感染源普通节点
           for (int i = 0; i < n; i++) {
               for (int j = 0; j < n; j++) {
                   if (graph[i][j] == 1 && !virus[i] && !virus[j]) {
                       merge(i, j);
                   }
               }
           }
           // 遍历感染源, 感染邻居节点
           for (int& i: initial) {
               for (int neighbor = 0; neighbor < n; neighbor++) {
                   if (i != neighbor && !virus[neighbor] && graph[i][neighbor] == 1) {
                       int fn = find(neighbor);
                       // 没被感染的节点，标记感染源
                       // 被再次感染的节点，变为失效
                       if (infect[fn] == -1) {
                           infect[fn] = i;
                       } else if (infect[fn] != -2 && infect[fn] != i) {
                           infect[fn] = -2;
                       }
                   }
               }
           }
           // 统计每个感染源节点的单独感染数
           for (int i = 0; i < n; i++) {
               if (i == find(i) && infect[i] >= 0) {
                   cnts[infect[i]] += setsize[i];
               }
           }
           // 找感染最多的病毒节点
           sort(initial.begin(), initial.end());
           int ans = initial[0];
           int maxinf = cnts[ans];
           for (int& i: initial) {
               if (cnts[i] > maxinf) {
                   ans = i;
                   maxinf = cnts[i];
               }
           }
           return ans;
       }
   
       void build(int n) {
           const auto& init = *initPtr;
           for (int i = 0; i < n; i++) {
               father[i] = i;
               setsize[i] = 1;
               virus[i] = false;
               infect[i] = -1; // -1为未感染
               cnts[i] = 0;
           }
           for (auto& i: init) { // 感染源
               virus[i] = true;
           }
       }
   
       int find(int i) {
           if (i != father[i]) {
               father[i] = find(father[i]);
           }
           return father[i];
       }
   
       void merge(int a, int b) {
           int fa = find(a);
           int fb = find(b);
           if (fa != fb) {
               if (setsize[fa] < setsize[fb]) {
                   father[fa] = father[fb];
                   setsize[fb] += setsize[fa];
               } else {
                   father[fb] = father[fa];
                   setsize[fa] += setsize[fb];
               }
           }
       }
   };
   ```

   