# 17双指针

## 题单

力扣: 简单 * 1;	中等 * 4;	困难 * 2;

洛谷: 中等 * 1;

1. [922. 按奇偶排序数组 II](https://leetcode.cn/problems/sort-array-by-parity-ii/) (simple)
2. [287. 寻找重复数](https://leetcode.cn/problems/find-the-duplicate-number/) (medium)
3. [42. 接雨水](https://leetcode.cn/problems/trapping-rain-water/) (hard)
4. [881. 救生艇](https://leetcode.cn/problems/boats-to-save-people/) (medium)
5. [11. 盛最多水的容器](https://leetcode.cn/problems/container-with-most-water/) (medium)
6. [475. 供暖器](https://leetcode.cn/problems/heaters/) (medium)
7. [41. 缺失的第一个正数](https://leetcode.cn/problems/first-missing-positive/) (hard)
8. [P17147](https://www.luogu.com.cn/problem/P17147) (普及)

## code

1. [922. 按奇偶排序数组 II](https://leetcode.cn/problems/sort-array-by-parity-ii/) (simple)

   ```c++
   class Solution {
   public:
       vector<int> sortArrayByParityII(vector<int>& nums) {
           int even = 0, odd = 1;
           int n = nums.size();
           while (even < n && odd < n) {
               if ((nums[n - 1] & 1) == 0) {
                   swap(nums, n - 1, even);
                   even += 2;
               } else {
                   swap(nums, n - 1, odd);
                   odd += 2;
               }
           }
           return nums;
       }
   
       void swap(vector<int>& nums, int i, int j) {
           int temp = nums[i];
           nums[i] = nums[j];
           nums[j] = temp;
       }
   };
   ```

   

2. [287. 寻找重复数](https://leetcode.cn/problems/find-the-duplicate-number/) (medium)

   > **思路：** 
   >
   > 双指针找入环节点
   >
   > 1. `f` 指针一次走2步，`s` 指针一次走1步，直到相遇，或走到尾；
   > 2. 相遇后，快指针回到开头，之后在相遇前，双指针每次走1步，相遇节点即入环节点

   ```c++
   class Solution {
   public:
       int findDuplicate(vector<int>& nums) {
           // 双指针,找入环节点
           int s = nums[0], f = nums[nums[0]];
           while (f != s) {
               f = nums[nums[f]];
               s = nums[s];
           }
           f = 0;
           while (f != s) {
               f = nums[f];
               s = nums[s];
           }
           return f;
       }
   };
   ```

3. [42. 接雨水](https://leetcode.cn/problems/trapping-rain-water/) (hard)

   > **思路：** 
   >
   > 1.  `i` 位置存储的水量，取决于其左侧和右侧的最大高度的短板，用2个变量记录双侧最大值；
   > 2. 用2个指针记录当前左右位置，`l` 从左往右， `r` 从右往左，直到相遇。
   > 3. 遍历过程中因为 `l` 左侧的最大高度确定，`r` 右侧最大高度确定，所以每次比较两测最大值，移动最大值较小一侧指针，更新答案并维护变量

   ```c++
   class Solution {
   public:
       int trap(vector<int>& height) {
           int n = height.size();
           // 双指针
           int sum = 0, l = 0, lmax = height[0], r = n - 1, rmax = height[n - 1];
           while (l <= r) {
               // 最大值小的一侧移动指针
               // 并更新答案，维护单侧最大值
               if (lmax <= rmax) {
                   sum += lmax - height[l++];
                   if (l < n) {
                       lmax = max(lmax, height[l]);
                   }
               } else {
                   sum += rmax - height[r--];
                   if (r >= 0) {
                       rmax = max(rmax, height[r]);
                   }
               }
           }
           return sum;
       }
   };
   ```

4. [881. 救生艇](https://leetcode.cn/problems/boats-to-save-people/) (medium)

   > 给定数组 `people` 。`people[i]`表示第 `i` 个人的体重 ，**船的数量不限**，每艘船可以承载的最大重量为 `limit`。
   >
   > 每艘船最多可同时载两人，但条件是这些人的重量之和最多为 `limit`。
   >
   > 返回 *承载所有人所需的最小船数* 。

   > **思路：** 
   >
   > 1. 排序 `people` 数组，双指针 `l` 指向剩余体重最大和 `r` 指向剩余体重最小的人，若可以同船，则一起走，否则体重最大的单独一艘船。
   > 2. `l` 左边的，一定是和 `r` 右边的体重更大的人一起走的，一定用了最少的船，所以贪心成立

   ```c++
   class Solution {
   public:
       int numRescueBoats(vector<int>& people, int limit) {
           int ans = 0;
           int l = 0, r = people.size() - 1, sum;
           sort(people.begin(), people.end());
           while (l <= r) {
               sum = l == r ? people[l] : people[l] + people[r]; // 左右体重和
               if (sum <= limit) {
                   l++, r--;
                   ans++;
               } else { // 最大体重的无法和现有最小体重同船
                   r--;
                   ans++;
               }
           }
           return ans;
       }
   };
   ```

5. [11. 盛最多水的容器](https://leetcode.cn/problems/container-with-most-water/) (medium)

   > 给定一个长度为 `n` 的整数数组 `height` 。有 `n` 条垂线，第 `i` 条线的两个端点是 `(i, 0)` 和 `(i, height[i])` 。
   >
   > 找出其中的两条线，使得它们与 `x` 轴共同构成的容器可以容纳最多的水。
   >
   > 返回容器可以储存的最大水量。
   >
   > **说明：**你不能倾斜容器。

   ```c++
   class Solution {
   public:
       int maxArea(vector<int>& height) {
           int ans = 0;
           int l = 0, r = height.size() - 1;
           while (l <= r) {
               ans = max(ans, min(height[l], height[r]) * (r - l));
               // 盛水量瓶颈取决于最小项，
               // 所以只移动height小的指针，不会错过最优答案
               if (height[l] <= height[r]) {
                   l++;
               } else {
                   r--;
               }
           }
           return ans;
       }
   };
   ```

6. [475. 供暖器](https://leetcode.cn/problems/heaters/) (medium)

   > 冬季已经来临。 你的任务是设计一个有固定加热半径的供暖器向所有房屋供暖。
   >
   > 在加热器的加热半径范围内的每个房屋都可以获得供暖。
   >
   > 现在，给出位于一条水平线上的房屋 `houses` 和供暖器 `heaters` 的位置，请你找出并返回可以覆盖所有房屋的最小加热半径。
   >
   > **注意**：所有供暖器 `heaters` 都遵循你的半径标准，加热的半径也一样。

   > **思路：** 
   >
   > 1. 先分别对房屋，供暖器排序。
   > 2. 排序后，匹配  `i` 号房屋和 `j` 号供暖器，因为已经对它们排序过，若当前不是 `i` 号房屋的最优匹配，那一定是 `j + 1` 号供暖器的相对距离更短，情况更优；而只有在有更优匹配时移动， `j - 1` 一定不会更好。同时，因为排序过，`i + 1` 号房屋距离 `j - 1` 号供暖器一定大于 `i + 1` 号房屋和 `j ` 号供暖器的距离，也不会错过 `i + 1` 号房的最优情况。

   ```c++
   class Solution {
   public:
       int findRadius(vector<int>& houses, vector<int>& heaters) {
           sort(houses.begin(), houses.end());
           sort(heaters.begin(), heaters.end());
           int ans = 0;
           // 双指针i, j指向房屋和供暖器
           for (int i = 0, j = 0; i < houses.size(); i++) {
               while (j + 1 < heaters.size() && 
               abs(houses[i] - heaters[j]) >= abs(houses[i] - heaters[j + 1])) { // i号房屋能匹配到的还不是最优供暖器
                   j++;
               }
               ans = max(ans, abs(houses[i] - heaters[j]));
           }
           return ans;
       }
   };
   ```

7. [41. 缺失的第一个正数](https://leetcode.cn/problems/first-missing-positive/) (hard)

   > 给你一个未排序的整数数组 `nums` ，请你找出其中没有出现的最小的正整数。
   >
   > 请你实现时间复杂度为 $O(n)$, 并且只使用常数级别额外空间的解决方案。

   > **思路：** 
   >
   > 1. `l` 左侧是有效数字区域，`r` 右侧为无效数字区域，有序的正整数应该时 `i` 位置上时 `i + 1`；不满足就交换，尽量让它去应该去的位置。
   > 2. 遇到 $nums[l] <= l$，说明左侧有效区域已经收集过这个数了，这是一个无效数字；
   > 3. 遇到 $nums[l] > r + 1​$，说明这个数字已经超出最优可能有序的范围，也是无效数字；
   > 4. 遇到 $nums[nums[l] - 1] == nums[l]$，说明这数字该交换到的位置已经有一个重复的数了，那现在这就也是一个无效的数字

   ```c++
   class Solution {
   public:
       int firstMissingPositive(vector<int>& nums) {
           // l左侧都是有效数字，r右侧为无效数字区域
           int l = 0, r = nums.size() - 1;
           while (l <= r) {
               if (nums[l] == l + 1) { // 在位置上
                   l++;
               } else if (nums[l] <= l || nums[l] > r + 1 || nums[nums[l] - 1] == nums[l]) { // 无效区域扩充的情况
                   swap(nums, l, r--);
               } else { // 归位
                   swap(nums, l, nums[l] - 1);
               }
           }
           return l + 1;
       }
   
       void swap(vector<int>& arr, int i, int j) {
           int temp = arr[i];
           arr[i] = arr[j];
           arr[j] = temp;
       }
   };
   ```

8. [P17147](https://www.luogu.com.cn/problem/P17147) (普及)

   > # P17147 [ICPC 2017 Xi'an R] Lovers
   >
   > 1. 题目描述
   >
   > 一天，$n$ 个女孩和 $n$ 个男孩来到西安寻找伴侣。每个女孩有一个价值 $a[i]$，每个男孩有一个价值 $b[i]$。只有当 $a[i] + b[j] \ge k$ 时，女孩 $i$ 和男孩 $j$ 才会坠入爱河。
   >
   > 请帮助他们尽可能多地配对。
   >
   > 2. 输入格式
   >
   > 多组测试数据。
   >
   > 第一行包含一个整数 $T$（$1 \le T \le 10$），表示测试数据组数。
   >
   > 接下来是 $T​$ 组数据。每组数据以两个整数 $N​$、$K​$ 开始（$1 \le N \le 200000​$，$0 \le K \le 10^9​$）。接下来一行包含 $N​$ 个整数，依次表示 $a[1]​$ 到 $a[N]​$（$0 \le a[i] \le 10^9​$）。再接下来一行包含 $N​$ 个整数，依次表示 $b[1]​$ 到 $b[N]​$（$0 \le b[i] \le 10^9​$）。
   >
   > 3. 输出格式
   >
   > 对于每组测试数据，在一行中输出答案。

   > **思路：** 
   >
   > 1. 贪心，排序两个数组后，用双指针。`l` 找 `a[]` 最小元素，`r` 找 `b[]` 最大元素
   > 2. 若当前的2个元素无法匹配，则 `l` 移动，舍弃当前的 `a[]`。`r` 右边的 `b[]` 一定和更小的 `a[]` 配对了，配对一定更多。

   ```c++
   #include <iostream>
   #include <vector>
   #include <algorithm>
   
   using namespace std;
   
   enum {
       MAXN = 200001
   };
   static vector<int> a = vector<int>(MAXN, 0);
   static vector<int> b = vector<int>(MAXN, 0);
   int sp(int n, int k);
   
   int main() {
       int t;
       cin >> t; // t组
       for (int turn = 0; turn < t; turn++) {
           int n, k; // n个数，目标值k
           cin >> n >> k;
           for (int i = 0; i < n; i++) {
               cin >> a[i];
           }
           for (int i = 0; i < n; i++) {
               cin >> b[i];
           }
           cout << sp(n , k) << '\n';
       }
   }
   
   // a,b有效长n，最多配对
   int sp(int n, int k) {
       sort(a.begin(), a.begin() + n);
       sort(b.begin(), b.begin() + n);
       int l = 0, r = n - 1;
       int ans = 0;
       // 排序后双指针贪心
       while (l < n) {
           if (a[l] + b[r] >= k) {
               ans++;
               l++, r--;
           } else { //a[l]值太小，当前最大b和它都无法满足
               l++;
           }
       }
       return ans;
   }
   ```

   

